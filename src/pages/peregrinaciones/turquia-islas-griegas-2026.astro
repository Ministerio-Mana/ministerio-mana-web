---
import CumbreLayout from '@components/cumbre2026/CumbreLayout.astro';
import ManaStoryDeck from '@components/story/ManaStoryDeck.astro';
import bookOpenCheckIcon from 'lucide-static/icons/book-open-check.svg?raw';
import calendarIcon from 'lucide-static/icons/calendar.svg?raw';
import clockIcon from 'lucide-static/icons/clock.svg?raw';
import contactIcon from 'lucide-static/icons/contact.svg?raw';
import mapIcon from 'lucide-static/icons/map.svg?raw';
import planeIcon from 'lucide-static/icons/plane.svg?raw';
import shipIcon from 'lucide-static/icons/ship.svg?raw';
import ticketIcon from 'lucide-static/icons/ticket.svg?raw';
import walletIcon from 'lucide-static/icons/wallet.svg?raw';

const lucideIcon = (svg: string) =>
  svg
    .replace(/<!--[\s\S]*?-->\s*/g, '')
    .replace(/<svg\b([^>]*)class="([^"]*)"([^>]*)>/, '<svg$1class="pilgrim-lucide-icon $2"$3 aria-hidden="true" focusable="false">')
    .replace(/<svg\b((?:(?!class=)[^>])*)>/, '<svg$1 class="pilgrim-lucide-icon" aria-hidden="true" focusable="false">');

const assetBase = '/images/peregrinaciones/turquia-islas-griegas-2026';
const flagImage = `${assetBase}/turkey-flag.jpg`;
const posterImage = `${assetBase}/poster-reference.jpg`;
const routeMapImage = `${assetBase}/route-map-original.jpg`;
const pilgrimageVideoId = 'nQ8CZT5aerA';
const pilgrimageVideoEmbed = `https://www.youtube-nocookie.com/embed/${pilgrimageVideoId}?rel=0&modestbranding=1&playsinline=1`;

const whatsappText = encodeURIComponent(
  'Hola Ministerio Maná, quiero información de la peregrinación a Turquía e Islas Griegas 2026.',
);
const whatsappHref = `https://wa.me/573004298001?text=${whatsappText}`;
const whatsappSecondaryHref = `https://wa.me/573148297534?text=${whatsappText}`;

const quickFacts = [
  { label: 'Fecha', value: '17 oct - 1 nov 2026', icon: lucideIcon(calendarIcon) },
  { label: 'Duración', value: '16 días', icon: lucideIcon(clockIcon) },
  { label: 'Ruta', value: 'Turquía + Islas Griegas', icon: lucideIcon(mapIcon) },
  { label: 'Enfoque', value: 'Las siete iglesias', icon: lucideIcon(bookOpenCheckIcon) },
];

const routeStops = [
  'Estambul',
  'Esmirna',
  'Kuşadası',
  'Patmos',
  'Rodas',
  'Creta',
  'Santorini',
  'Atenas',
  'Mykonos',
  'Éfeso',
  'Pérgamo',
  'Tiatira',
  'Sardes',
  'Filadelfia',
  'Pamukkale',
  'Laodicea',
  'Konya',
  'Capadocia',
];

const routePillars = [
  {
    title: 'Palabra',
    text: 'Una ruta para caminar escenarios bíblicos y leer la historia con propósito.',
  },
  {
    title: 'Comunión',
    text: 'Un tiempo para viajar como iglesia, compartir mesa y crecer juntos.',
  },
  {
    title: 'Preparación',
    text: 'Acompañamiento para cupos, requisitos, tiquetes y claridad logística.',
  },
];

const itinerary = [
  {
    day: '01',
    date: '17 oct',
    title: 'Salida del país',
    meta: 'Inicio de la peregrinación',
    body: 'Salida desde tu lugar de origen con destino a Estambul. ¡Comienza nuestra peregrinación a Turquía e Islas Griegas!',
  },
  {
    day: '02',
    date: '18 oct',
    title: 'Llegada a Estambul',
    meta: 'Cena',
    body: 'Llegada a Estambul y traslado al hotel. Cena y alojamiento.',
  },
  {
    day: '03',
    date: '19 oct',
    title: 'Estambul - Izmir',
    meta: 'D - A - C',
    body: 'Bazar Egipcio, mercado de las especias. Recorrido en barco por el Bósforo, entre Europa y Asia, con sus palacios y yalıs. Almuerzo, traslado al aeropuerto y vuelo a Esmirna. Cena y alojamiento.',
  },
  {
    day: '04',
    date: '20 oct',
    title: 'Embarque al crucero',
    meta: 'Kuşadası / Patmos · D - A - C',
    body: 'Desayuno. Traslado al puerto de Kuşadası para embarcar en el crucero. Salida hacia la isla de Patmos: visita al monasterio y a la gruta donde el apóstol Juan escribió el Apocalipsis.',
  },
  {
    day: '05',
    date: '21 oct',
    title: 'Crucero: Rodas',
    meta: 'D - A - C',
    body: 'Llegada al puerto de Rodas, isla de los Caballeros de la Orden de San Juan. Excursión opcional a la Acrópolis de Lindos y a la ciudad medieval, Antigua Rodas.',
  },
  {
    day: '06',
    date: '22 oct',
    title: 'Crucero: Creta / Santorini',
    meta: 'D - A - C',
    body: 'Parada en Heraklio, Creta, con excursión opcional al Palacio de Knosos, centro de la civilización minoica. Navegación a Santorini, el continente perdido de la Atlántida. Excursión opcional a Oía, uno de los lugares más mágicos para ver la puesta de sol.',
  },
  {
    day: '07',
    date: '23 oct',
    title: 'Crucero: Atenas / Mykonos',
    meta: 'D - A - C',
    body: 'Por la mañana, excursión opcional a Atenas. Por la tarde, parada en Mykonos, famosa por sus playas, molinos de viento y encanto.',
  },
  {
    day: '08',
    date: '24 oct',
    title: 'Kuşadası - Éfeso - Izmir',
    meta: 'Desayuno y almuerzo',
    body: 'Desembarque y visita de Éfeso, una de las siete iglesias, desde donde el apóstol Pablo escribió a los Corintios y a los Gálatas: el Teatro, la Biblioteca de Celso y la Vía Mármol. Almuerzo, Basílica de San Juan, dedicada al apóstol Juan, autor del Apocalipsis. Centro de cuero. Alojamiento en Kuşadası.',
  },
  {
    day: '09',
    date: '25 oct',
    title: 'Izmir - Pérgamo - Tiatira',
    meta: 'Desayuno y cena',
    body: 'Pérgamo: Acrópolis con su Gran Teatro y los Templos de Atenea y Zeus. Basílica Roja, la primera de las siete iglesias. Tiatira, la segunda de las siete iglesias, con el templo dedicado a Apolo. Regreso a Izmir, cena y alojamiento.',
  },
  {
    day: '10',
    date: '26 oct',
    title: 'Esmirna - Sardes - Filadelfia',
    meta: 'Pamukkale · D - A - C',
    body: 'Iglesia de Policarpo en Esmirna, la tercera de las siete iglesias; obispo y mártir cristiano del siglo II. Sardes, la madre de las siete iglesias, donde el rey Creso acuñó la primera moneda de la historia: templo, iglesia bizantina, gimnasio y sinagoga. Almuerzo. Filadelfia, la sexta de las siete iglesias, con su basílica bizantina. Continuación a Pamukkale. Cena y alojamiento.',
  },
  {
    day: '11',
    date: '27 oct',
    title: 'Pamukkale',
    meta: 'D - A - C',
    body: 'Pamukkale, cuyas aguas calcáreas han creado formaciones espectaculares. Hierápolis: tumba del apóstol Felipe, teatro y tiempo en aguas termales. Laodicea, una de las siete iglesias evangelizada por Epafras. Almuerzo, cena y alojamiento.',
  },
  {
    day: '12',
    date: '28 oct',
    title: 'Pamukkale - Konya - Capadocia',
    meta: 'D - A - C',
    body: 'Konya, Iconium, donde los apóstoles Pablo y Bernabé predicaron el Evangelio. Almuerzo. Continuación a Capadocia; en ruta, un kervansaray del siglo XIII de la Ruta de la Seda. Cena y alojamiento.',
  },
  {
    day: '13',
    date: '29 oct',
    title: 'Capadocia',
    meta: 'D - A - C',
    body: 'Al amanecer, paseo en globo aerostático sobre las chimeneas de hadas, incluido. Valle de Göreme, Uçhisar, Avcilar y Valle de Derbent. Almuerzo, talleres de alfombras y ónyx. Cena y espectáculo de danza folclórica.',
  },
  {
    day: '14',
    date: '30 oct',
    title: 'Vuelo a Estambul',
    meta: 'Sultanahmet · D - A - C',
    body: 'Vuelo a Estambul. Barrios de Pera y Gálata: Torre de Gálata, exterior, Plaza Taksim y avenida İstiklal. Almuerzo. Barrio de Sultanahmet: Hipódromo Romano, Mezquita Azul, 6 minaretes, y Santa Sofía, siglo VI. Cena y alojamiento.',
  },
  {
    day: '15',
    date: '31 oct',
    title: 'Estambul',
    meta: 'Topkapi y Gran Bazar · D - A - C',
    body: 'Palacio Topkapi, residencia de los sultanes otomanos, con entrada incluida. Patriarcado de Constantinopla en Fener; si está cerrado, se visita San Esteban de los Búlgaros. Barrios de Fener y Balat, Mezquita de Süleymaniye y tiempo libre en el Gran Bazar.',
  },
  {
    day: '16',
    date: '1 nov',
    title: 'Salida de Estambul',
    meta: 'Desayuno',
    body: 'Desayuno y traslado al aeropuerto de Estambul. Fin de nuestros servicios.',
  },
];

const hotels = [
  { city: 'Crucero', hotel: 'Celestyal - cabina interior, categoría IB / IC' },
  { city: 'Esmirna', hotel: 'Kaya Izmir Termal o similar' },
  { city: 'Pamukkale', hotel: 'Colossae Thermal o Adempira Thermal o similar' },
  { city: 'Capadocia', hotel: 'Utopia Hotel Cueva o similar' },
  { city: 'Estambul', hotel: 'Wish More o Windsor o similar' },
];

const includedTurkey = [
  '3 noches en Estambul, 2 en Capadocia, 2 en Pamukkale y 3 en Esmirna.',
  'Todos los traslados en grupo, en bus privado con aire acondicionado.',
  'Todos los tours con guía de habla española.',
  '10 desayunos, 9 almuerzos y 10 cenas.',
  'Vuelo doméstico Capadocia - Estambul con 20 kg + 8 kg de cabina.',
  'Noche de bailes folclóricos y paseo en globo en Capadocia.',
  'Entradas a los museos y auriculares.',
  'Nueva tasa de alojamiento en los hoteles de Turquía.',
];

const includedCruise = [
  'Crucero en régimen de pensión completa en cabina interior categoría IB.',
  'Café, té, agua y jugo ilimitados en el horario del restaurante, 6:30-22:00.',
  'Refrescos ilimitados con las comidas en restaurantes Thalassa y Taverna.',
  'Impuestos y propinas del crucero.',
];

const notIncluded = [
  'Gastos personales.',
  'Bebidas durante los almuerzos y las cenas en Turquía.',
  'Traslados individuales por llegadas o salidas fuera de los vuelos del grupo.',
  'Propinas al guía y al conductor: sugerido 3-5 USD por persona/día al guía y 2-3 USD al chofer.',
  'Las excursiones del crucero en Patmos y Atenas, y otras excursiones.',
  'Bebidas durante el crucero.',
  'Gastos personales y todo lo no mencionado expresamente como incluido.',
];

const flightNotes = [
  'Es importante tener en cuenta que el Ministerio Maná no es una agencia de viajes; por ello, los tiquetes aéreos son adquiridos a través de una agencia, y nosotros actuamos como intermediarios entre la agencia y el peregrino.',
  'Cada persona es libre de comparar los tiquetes de forma particular. Nosotros ayudamos a gestionar sus vuelos, escalas y tiempos, pero la decisión final la toma cada peregrino.',
  'Los valores enviados son válidos solo por 24 horas; pasado este tiempo, se debe volver a cotizar. Si desea negociar directamente con la agencia, puede hacerlo.',
  'El Ministerio Maná no cobra ningún valor por la intermediación; por lo tanto, si el itinerario sufre algún cambio que implique un sobrecosto, este deberá ser asumido por cada viajero.',
  'En caso de devoluciones relacionadas con la porción aérea, cada viajero deberá asumir los gastos bancarios correspondientes, que oscilan entre el 3% y el 5%, según la comisión de cada banco.',
  'Los cambios que realice la aerolínea, modificación del día, cambio de horario o cancelación de escalas, entre otros, deberán ser asumidos por cada peregrino, ya que están fuera de nuestro alcance.',
  'Al reservar el cupo de la peregrinación, usted nos autoriza a realizar la compra de los tiquetes. Si por motivos personales decide cancelar su viaje, los tiquetes quedarán abiertos para usarse dentro del período que determine la aerolínea, generalmente hasta un año, por lo que el Ministerio Maná no podrá hacer devolución de dinero por este concepto.',
];

const travelCare = [
  {
    title: 'Pasaporte',
    body: 'Debe estar en buen estado, con vigencia mínima de 6 meses y al menos 5 hojas en blanco.',
  },
  {
    title: 'Aeropuerto',
    body: 'Llegar a la hora indicada, llevar pasaporte, documento de identidad y dinero siempre en bolsa de mano.',
  },
  {
    title: 'Durante el vuelo',
    body: 'Conservar siempre el pase de abordaje y registrarse por internet antes del viaje si desea redimir millas.',
  },
];

const itineraryPreview = itinerary.slice(0, 6);
const includedPreview = includedTurkey.slice(0, 4);
const notIncludedPreview = notIncluded.slice(0, 3);
const travelCareWithIcons = [
  { ...travelCare[0], icon: lucideIcon(contactIcon) },
  { ...travelCare[1], icon: lucideIcon(planeIcon) },
  { ...travelCare[2], icon: lucideIcon(ticketIcon) },
];

const structuredData = {
  '@context': 'https://schema.org',
  '@type': 'TouristTrip',
  name: 'Peregrinación Turquía e Islas Griegas 2026',
  description: 'Ruta de peregrinación del Ministerio Maná por Turquía, las siete iglesias e Islas Griegas.',
  image: `https://ministeriomana.org${posterImage}`,
  itinerary: routeStops.join(', '),
};

const cspNonce = Astro.locals?.cspNonce;
---

<CumbreLayout
  title="Peregrinación Turquía e Islas Griegas 2026 | Ministerio Maná"
  description="Ruta de peregrinación por Turquía, las siete iglesias e Islas Griegas del 17 de octubre al 1 de noviembre de 2026."
  image={posterImage}
  imageAlt="Arte de la peregrinación Turquía e Islas Griegas 2026"
  structuredData={structuredData}
  flush
  showEventBadge={false}
  showBackButton
  showAccountButton={false}
  backHref="/peregrinaciones/"
  backLabel="Peregrinaciones"
  useHistoryBack={false}
>
  <div class="pilgrim-page" data-pilgrim-story-root>
    <ManaStoryDeck
      class="pilgrim-story"
      label="Historia de la peregrinación a Turquía e Islas Griegas"
      panelCount={6}
      preset="editorial"
      staticMobile
      staticBreakpoint={520}
      scrollFactorBreakpoint={520}
      snapDirectional={false}
      snapInertia={false}
    >
      <section class="cumbre-panel pilgrim-panel pilgrim-panel--hero pilgrim-hero" aria-labelledby="peregrinacion-title" data-cumbre-panel data-panel-theme="dark">
        <div class="pilgrim-panel-bg" data-panel-bg aria-hidden="true"></div>
        <div class="pilgrim-panel-inner pilgrim-shell pilgrim-hero__grid">
          <div class="pilgrim-hero__copy">
            <p class="pilgrim-kicker" data-reveal>Peregrinación 2026</p>
            <h1 id="peregrinacion-title" data-split-title>Turquía e Islas Griegas</h1>
            <p class="pilgrim-script" data-reveal>Las Siete Iglesias</p>
            <p class="pilgrim-hero__lead" data-reveal>
              Una ruta para caminar la Palabra, reconocer lugares bíblicos y vivir comunión
              como familia Maná.
            </p>
            <div class="pilgrim-actions" data-reveal>
              <a href={whatsappHref} target="_blank" rel="noopener noreferrer" class="pilgrim-button pilgrim-button--teal">
                Quiero información
              </a>
              <a href="#itinerario" class="pilgrim-button pilgrim-button--line" data-story-link>Ver itinerario</a>
            </div>
          </div>

          <div class="pilgrim-hero__media" data-reveal>
            <article class="pilgrim-video-card" aria-label="Video de la peregrinación">
              <div class="pilgrim-video-card__topline">
                <span>Video peregrinación</span>
                <strong>2026</strong>
              </div>
              <div class="pilgrim-video-frame">
                <iframe
                  src={pilgrimageVideoEmbed}
                  title="Video de la peregrinación Turquía e Islas Griegas 2026"
                  loading="lazy"
                  allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share"
                  allowfullscreen
                ></iframe>
              </div>
              <p>Conoce la ruta, el enfoque bíblico y la experiencia antes de reservar tu lugar.</p>
            </article>

            <div class="pilgrim-poster pilgrim-poster--stamp" aria-hidden="true">
              <img src={posterImage} alt="" loading="eager" />
            </div>
          </div>
        </div>
      </section>

      <section id="ruta" class="cumbre-panel pilgrim-panel pilgrim-panel--route pilgrim-route" data-cumbre-panel data-panel-theme="dark">
        <div class="pilgrim-panel-bg" data-panel-bg aria-hidden="true"></div>
        <div class="pilgrim-panel-inner pilgrim-shell pilgrim-route__grid">
          <div class="pilgrim-route__content">
            <p class="pilgrim-kicker" data-reveal>La ruta</p>
            <h2 class="pilgrim-display-title pilgrim-display-title--route" data-reveal>
              <span class="pilgrim-display-title__outline">Ruta</span>
              <span class="pilgrim-display-title__solid">Bíblica</span>
              <span class="pilgrim-display-title__script">Turquía e Islas Griegas</span>
            </h2>
            <p data-reveal>
              Una peregrinación para recorrer lugares bíblicos, caminar la historia de la iglesia
              primitiva y vivir la ruta con acompañamiento pastoral, logística clara y comunidad.
            </p>
            <div class="pilgrim-route__motifs" data-reveal>
              {routePillars.map((pillar) => (
                <article>
                  <strong>{pillar.title}</strong>
                  <span>{pillar.text}</span>
                </article>
              ))}
            </div>
          </div>

          <div class="pilgrim-route__map" data-reveal>
            <img
              src={routeMapImage}
              alt="Mapa de la ruta de peregrinación por Turquía con Estambul, Esmirna, Capadocia y Colossae."
              width="945"
              height="570"
              loading="lazy"
              decoding="async"
            />
            <span class="pilgrim-route__map-caption">Ruta peregrinación</span>
          </div>

          <div class="pilgrim-facts pilgrim-facts--panel" aria-label="Datos principales" data-reveal>
            {quickFacts.map((fact) => (
              <article>
                <span class="pilgrim-fact-icon" set:html={fact.icon}></span>
                <span class="pilgrim-fact-label">{fact.label}</span>
                <strong>{fact.value}</strong>
              </article>
            ))}
          </div>

          <div class="pilgrim-route__stops" data-reveal>
            {routeStops.slice(0, 10).map((stop) => <span>{stop}</span>)}
          </div>
        </div>
      </section>

      <section id="itinerario" class="cumbre-panel pilgrim-panel pilgrim-panel--itinerary pilgrim-section pilgrim-section--navy" data-cumbre-panel data-panel-theme="dark">
        <div class="pilgrim-panel-bg" data-panel-bg aria-hidden="true"></div>
        <div class="pilgrim-panel-inner pilgrim-shell pilgrim-itinerary__grid">
          <div class="pilgrim-section-head pilgrim-section-head--story">
            <p class="pilgrim-kicker" data-reveal>Itinerario</p>
            <h2 class="pilgrim-display-title pilgrim-display-title--itinerary" data-reveal>
              <span class="pilgrim-display-title__outline">Día</span>
              <span class="pilgrim-display-title__solid">Por Día</span>
              <span class="pilgrim-display-title__script">paso a paso</span>
            </h2>
            <p data-reveal>D = desayuno, A = almuerzo, C = cena. Algunas excursiones del crucero son opcionales.</p>
          </div>

          <div class="pilgrim-itinerary__side">
            <div class="pilgrim-days pilgrim-days--preview" data-reveal>
              {itineraryPreview.map((item) => (
                <article class="pilgrim-day pilgrim-day--preview">
                  <div class="pilgrim-day__badge">
                    <span>Día</span>
                    <strong>{item.day}</strong>
                  </div>
                  <div class="pilgrim-day__body">
                    <p>{item.meta}</p>
                    <h3>{item.title}</h3>
                  </div>
                  <div class="pilgrim-day__date">{item.date}</div>
                </article>
              ))}
            </div>

            <div class="pilgrim-actions" data-reveal>
              <a href="#itinerario-detalle" class="pilgrim-button pilgrim-button--teal">Ver itinerario completo</a>
            </div>
          </div>
        </div>
      </section>

      <section id="hoteles" class="cumbre-panel pilgrim-panel pilgrim-panel--hotels pilgrim-hotels" data-cumbre-panel data-panel-theme="dark">
        <div class="pilgrim-panel-bg" data-panel-bg aria-hidden="true"></div>
        <div class="pilgrim-panel-inner pilgrim-shell pilgrim-hotels__grid">
          <div>
            <p class="pilgrim-kicker" data-reveal>Hotelería</p>
            <h2 class="pilgrim-display-title pilgrim-display-title--hotels" data-reveal>
              <span class="pilgrim-display-title__outline">Descanso</span>
              <span class="pilgrim-display-title__solid">Previsto</span>
              <span class="pilgrim-display-title__script">en la ruta</span>
            </h2>
            <p data-reveal>Hoteles de primera categoría y turista superior. El check-in es a partir de las 4:00 p.m.</p>
          </div>
          <div class="pilgrim-hotels__list" data-reveal>
            {hotels.map((item, index) => (
              <article>
                <em>{String(index + 1).padStart(2, '0')}</em>
                <div>
                  <span>{item.city}</span>
                  <strong>{item.hotel}</strong>
                </div>
              </article>
            ))}
          </div>
        </div>
      </section>

      <section id="incluye" class="cumbre-panel pilgrim-panel pilgrim-panel--includes pilgrim-section pilgrim-section--navy" data-cumbre-panel data-panel-theme="dark">
        <div class="pilgrim-panel-bg" data-panel-bg aria-hidden="true"></div>
        <div class="pilgrim-panel-inner pilgrim-shell pilgrim-includes-panel__grid">
          <div class="pilgrim-section-head pilgrim-section-head--story">
            <p class="pilgrim-kicker" data-reveal>Cobertura del viaje</p>
            <h2 class="pilgrim-display-title pilgrim-display-title--includes" data-reveal>
              <span class="pilgrim-display-title__outline">Incluye</span>
              <span class="pilgrim-display-title__solid">Y Aparte</span>
              <span class="pilgrim-display-title__script">con claridad</span>
            </h2>
            <p data-reveal>Lo cubierto por la ruta y lo que debes presupuestar por separado, sin mezclar los conceptos.</p>
          </div>

          <div class="pilgrim-includes-panel__side">
            <div class="pilgrim-coverage-board" data-reveal>
              <article class="pilgrim-coverage-card pilgrim-coverage-card--turkey">
                <span class="pilgrim-overview-icon" set:html={lucideIcon(mapIcon)}></span>
                <span class="pilgrim-overview-label">Incluye</span>
                <h3>Ruta en Turquía</h3>
                <ul>
                  {includedPreview.map((item) => <li>{item}</li>)}
                </ul>
              </article>
              <article class="pilgrim-coverage-card pilgrim-coverage-card--cruise">
                <span class="pilgrim-overview-icon" set:html={lucideIcon(shipIcon)}></span>
                <span class="pilgrim-overview-label">Crucero</span>
                <h3>Pensión completa</h3>
                <ul>
                  {includedCruise.map((item) => <li>{item}</li>)}
                </ul>
              </article>
              <article class="pilgrim-coverage-card pilgrim-coverage-card--apart">
                <span class="pilgrim-overview-icon" set:html={lucideIcon(walletIcon)}></span>
                <span class="pilgrim-overview-label">A presupuestar</span>
                <h3>No incluido</h3>
                <ul>
                  {notIncludedPreview.map((item) => <li>{item}</li>)}
                </ul>
              </article>
            </div>

            <div class="pilgrim-actions" data-reveal>
              <a href="#incluye-detalle" class="pilgrim-button pilgrim-button--teal">Ver detalle incluido</a>
              <a href="#tiquetes-detalle" class="pilgrim-button pilgrim-button--line">Tiquetes</a>
            </div>
          </div>
        </div>
      </section>

      <section class="cumbre-panel pilgrim-panel pilgrim-panel--final pilgrim-final" data-cumbre-panel data-panel-theme="light">
        <div class="pilgrim-panel-bg" data-panel-bg aria-hidden="true"></div>
        <div class="pilgrim-panel-inner pilgrim-shell pilgrim-final__box">
          <div>
            <p class="pilgrim-kicker pilgrim-kicker--navy" data-reveal>Da el primer paso</p>
            <h2 class="pilgrim-display-title pilgrim-display-title--final" data-reveal>
              <span class="pilgrim-display-title__outline">Reserva</span>
              <span class="pilgrim-display-title__solid">Tu Lugar</span>
              <span class="pilgrim-display-title__script">con el equipo</span>
            </h2>
            <p data-reveal>Te ayudamos a revisar cupos, requisitos, pasaporte, reserva y cotización aérea vigente.</p>
          </div>
          <div class="pilgrim-final__contact" data-reveal>
            <div class="pilgrim-final__stamp" aria-hidden="true">
              <span>17 oct</span>
              <strong>1 nov</strong>
              <em>2026</em>
            </div>
            <div class="pilgrim-final__actions">
              <a href={whatsappHref} target="_blank" rel="noopener noreferrer" class="pilgrim-button pilgrim-button--navy">
                WhatsApp 300 429 8001
              </a>
              <a href={whatsappSecondaryHref} target="_blank" rel="noopener noreferrer" class="pilgrim-button pilgrim-button--navy-line">
                WhatsApp 314 829 7534
              </a>
            </div>
          </div>
        </div>
      </section>
    </ManaStoryDeck>

    <section id="itinerario-detalle" class="pilgrim-section pilgrim-section--navy pilgrim-detail-section">
      <div class="pilgrim-shell">
        <div class="pilgrim-section-head">
          <p class="pilgrim-kicker">Itinerario</p>
          <h2 class="pilgrim-display-title pilgrim-display-title--detail">
            <span class="pilgrim-display-title__outline">Detalle</span>
            <span class="pilgrim-display-title__solid">Completo</span>
            <span class="pilgrim-display-title__script">día por día</span>
          </h2>
          <p>D = desayuno, A = almuerzo, C = cena. Algunas excursiones del crucero son opcionales.</p>
        </div>

        <div class="pilgrim-days">
          {itinerary.map((item) => (
            <article class="pilgrim-day">
              <div class="pilgrim-day__badge">
                <span>Día</span>
                <strong>{item.day}</strong>
              </div>
              <div class="pilgrim-day__body">
                <p>{item.meta}</p>
                <h3>{item.title}</h3>
                <small>{item.body}</small>
              </div>
              <div class="pilgrim-day__date">{item.date}</div>
            </article>
          ))}
        </div>
      </div>
    </section>

    <section id="incluye-detalle" class="pilgrim-section pilgrim-section--navy pilgrim-detail-section">
      <div class="pilgrim-shell">
        <div class="pilgrim-section-head pilgrim-section-head--center">
          <p class="pilgrim-kicker">Servicios incluidos</p>
          <h2 class="pilgrim-display-title pilgrim-display-title--detail pilgrim-display-title--center">
            <span class="pilgrim-display-title__outline">Qué Cubre</span>
            <span class="pilgrim-display-title__solid">La Ruta</span>
            <span class="pilgrim-display-title__script">y qué va aparte</span>
          </h2>
        </div>

        <div class="pilgrim-includes">
          <article>
            <h3>Parte Turquía</h3>
            <ul>
              {includedTurkey.map((item) => <li>{item}</li>)}
            </ul>
          </article>
          <article>
            <h3>Crucero</h3>
            <ul>
              {includedCruise.map((item) => <li>{item}</li>)}
            </ul>
          </article>
          <article class="pilgrim-includes__not">
            <h3>No se incluye</h3>
            <ul>
              {notIncluded.map((item) => <li>{item}</li>)}
            </ul>
          </article>
        </div>
      </div>
    </section>

    <section id="tiquetes-detalle" class="pilgrim-flights pilgrim-detail-section">
      <img src={flagImage} alt="" loading="lazy" data-parallax-bg data-bg="0.45" />
      <div class="pilgrim-flights__shade"></div>
      <div class="pilgrim-shell pilgrim-flights__content">
        <div class="pilgrim-flights__intro">
          <p class="pilgrim-kicker">Tiquetes aéreos</p>
          <h2 class="pilgrim-display-title pilgrim-display-title--flights">
            <span class="pilgrim-display-title__outline">Vuelos</span>
            <span class="pilgrim-display-title__solid">Con Claridad</span>
            <span class="pilgrim-display-title__script">sin reemplazar tu decisión</span>
          </h2>
        </div>
        <div class="pilgrim-flight-notes">
          {flightNotes.map((note) => <p>{note}</p>)}
        </div>
      </div>
    </section>

    <section id="preparacion-detalle" class="pilgrim-section pilgrim-section--navy pilgrim-detail-section">
      <div class="pilgrim-shell">
        <div class="pilgrim-section-head">
          <p class="pilgrim-kicker">Consideraciones importantes</p>
          <h2 class="pilgrim-display-title pilgrim-display-title--detail">
            <span class="pilgrim-display-title__outline">Antes</span>
            <span class="pilgrim-display-title__solid">De Salir</span>
            <span class="pilgrim-display-title__script">preparados</span>
          </h2>
        </div>
        <div class="pilgrim-care">
          {travelCareWithIcons.map((item) => (
            <article>
              <span class="pilgrim-care__icon" set:html={item.icon}></span>
              <div>
                <h3>{item.title}</h3>
                <p>{item.body}</p>
              </div>
            </article>
          ))}
        </div>
      </div>
    </section>

    <div class="pilgrim-mobile-cta">
      <a href={whatsappHref} target="_blank" rel="noopener noreferrer">Quiero información</a>
    </div>

    <script nonce={cspNonce} is:inline>
      (() => {
        const root = document.querySelector('[data-pilgrim-story-root]');
        if (!root) return;

        const scrollToStoryPanel = (targetPanel) => {
          const story = root.querySelector('[data-cumbre-story]');
          if (!story || !targetPanel) return false;

          const isAnimatedPanel = getComputedStyle(targetPanel).position === 'absolute';
          if (isAnimatedPanel) {
            const panels = Array.from(story.querySelectorAll('[data-cumbre-panel]'));
            const index = Math.max(0, panels.indexOf(targetPanel));
            const breakpoint = Number.parseInt(story.dataset.cumbreScrollFactorBreakpoint || '768', 10);
            const requestedFactor = Number.parseFloat(story.dataset.cumbreScrollFactor || '');
            const preset = story.dataset.storyPreset || 'editorial';
            const presetFactor = preset === 'cinematic' ? 1.34 : preset === 'calm' ? 1.08 : 1.2;
            const scrollFactor = window.innerWidth >= breakpoint
              ? (Number.isFinite(requestedFactor) ? requestedFactor : presetFactor)
              : 1;
            const viewportHeight = window.visualViewport?.height || window.innerHeight || 1;
            const storyTop = story.offsetTop || 0;
            const target = Math.max(storyTop, storyTop + viewportHeight * index * scrollFactor);

            if (window.lenis?.scrollTo) {
              window.lenis.scrollTo(target, { duration: 0.48, force: true });
              return true;
            }

            window.scrollTo({ top: target, behavior: 'smooth' });
            return true;
          }

          targetPanel.scrollIntoView({ behavior: 'smooth', block: 'start' });
          return true;
        };

        document.addEventListener('click', (event) => {
          if (!(event.target instanceof Element)) return;

          const link = event.target.closest('.pilgrim-page a[href^="#"]');
          if (!link) return;

          const href = link.getAttribute('href');
          if (!href || href === '#') return;

          const target = document.querySelector(href);
          if (!target) return;

          event.preventDefault();

          const handled = target.matches('[data-cumbre-panel]')
            ? scrollToStoryPanel(target)
            : false;

          if (!handled) {
            const top = Math.max(0, target.getBoundingClientRect().top + window.scrollY - 68);
            if (window.lenis?.scrollTo) {
              window.lenis.scrollTo(top, { duration: 0.48, force: true });
            } else {
              window.scrollTo({ top, behavior: 'smooth' });
            }
          }

          if (history.pushState) {
            history.pushState(null, '', href);
          }
        });
      })();
    </script>
    <script>
      import '@scripts/cumbre-welcome-story.js';
    </script>
  </div>
</CumbreLayout>

<style>
  @font-face {
    font-family: "Pilgrim Summer";
    src: url("/fonts/SUMMER FONT/Summer Loving Regular.otf") format("opentype");
    font-weight: 400;
    font-style: normal;
    font-display: swap;
  }

  :global(html) {
    scroll-padding-top: 68px;
  }

  .pilgrim-page {
    --pilgrim-navy: #17243c;
    --pilgrim-deep: #07111f;
    --pilgrim-teal: #08aebe;
    --pilgrim-teal-soft: #d9f7fa;
    --pilgrim-white: #ffffff;
    --pilgrim-ink: #101827;
    background: var(--pilgrim-navy);
    color: var(--pilgrim-white);
    font-family: "Intro", ui-sans-serif, system-ui, sans-serif;
    overflow-x: clip;
  }

  .pilgrim-page *,
  .pilgrim-page *::before,
  .pilgrim-page *::after {
    box-sizing: border-box;
  }

  .pilgrim-shell {
    width: min(1160px, calc(100% - 32px));
    margin-inline: auto;
  }

  .pilgrim-story {
    position: relative;
    height: var(--cumbre-vh, 100svh);
    min-height: var(--cumbre-vh, 100svh);
    overflow: hidden;
    background: var(--pilgrim-deep);
  }

  .pilgrim-page .cumbre-panel {
    position: absolute;
    inset: 0;
    width: 100%;
    height: var(--cumbre-vh, 100svh);
    min-height: var(--cumbre-vh, 100svh);
    overflow: hidden;
    pointer-events: none;
    clip-path: inset(100% 0% 0% 0%);
    will-change: clip-path, transform;
  }

  .pilgrim-page .cumbre-panel.pilgrim-hero,
  .pilgrim-page .cumbre-panel.pilgrim-route,
  .pilgrim-page .cumbre-panel.pilgrim-section,
  .pilgrim-page .cumbre-panel.pilgrim-hotels,
  .pilgrim-page .cumbre-panel.pilgrim-final {
    padding: 0;
  }

  .pilgrim-page .cumbre-panel:first-child,
  .pilgrim-page .cumbre-panel.is-visible {
    clip-path: inset(0% 0% 0% 0%);
  }

  .pilgrim-page .cumbre-panel.is-active {
    pointer-events: auto;
  }

  .pilgrim-page .cumbre-panel a,
  .pilgrim-page .cumbre-panel button {
    pointer-events: auto;
  }

  .pilgrim-panel-bg {
    position: absolute;
    inset: 0;
    z-index: 0;
    pointer-events: none;
    transform-origin: center;
    will-change: transform;
  }

  .pilgrim-panel-inner {
    position: relative;
    z-index: 1;
    min-height: var(--cumbre-vh, 100svh);
    height: var(--cumbre-vh, 100svh);
    align-content: center;
    padding: 84px 0 60px;
  }

  .pilgrim-page [data-reveal] {
    will-change: transform, opacity;
  }

  .pilgrim-page :global(.pilgrim-lucide-icon) {
    width: 1em;
    height: 1em;
    stroke: currentColor;
    stroke-width: 1.8;
  }

  .pilgrim-page [data-split-title] .split-word {
    display: inline-block;
    overflow: hidden;
    vertical-align: top;
  }

  .pilgrim-page [data-split-title] .split-word > span {
    display: inline-block;
    will-change: transform, opacity;
  }

  :global(html[data-cumbre-static-story='true']) .pilgrim-story {
    height: auto;
    min-height: 0;
    overflow: visible;
  }

  :global(html[data-cumbre-static-story='true']) .pilgrim-page .cumbre-panel {
    position: relative;
    height: auto;
    min-height: 100svh;
    clip-path: none !important;
    pointer-events: auto;
  }

  :global(html[data-cumbre-static-story='true']) .pilgrim-panel-inner {
    height: auto;
    min-height: 100svh;
  }

  .pilgrim-hero {
    min-height: 100svh;
    display: grid;
    align-items: center;
    padding: 112px 0 72px;
    background:
      linear-gradient(180deg, rgba(7, 17, 31, 0.92), rgba(23, 36, 60, 0.98)),
      radial-gradient(circle at 20% 18%, rgba(8, 174, 190, 0.18), transparent 28%);
    overflow: hidden;
  }

  .pilgrim-hero__grid {
    display: grid;
    grid-template-columns: minmax(0, 1.08fr) minmax(320px, 0.72fr);
    gap: 44px;
    align-items: center;
  }

  .pilgrim-hero__copy {
    min-width: 0;
    max-width: 100%;
  }

  .pilgrim-kicker {
    margin: 0 0 16px;
    color: var(--pilgrim-teal);
    font-size: 0.8rem;
    font-weight: 700;
    letter-spacing: 0.42em;
    text-transform: uppercase;
  }

  .pilgrim-kicker--navy {
    color: var(--pilgrim-navy);
  }

  .pilgrim-hero h1,
  .pilgrim-route__content h2,
  .pilgrim-section-head h2,
  .pilgrim-hotels h2,
  .pilgrim-flights h2,
  .pilgrim-final h2 {
    margin: 0;
    font-family: "Intro", ui-sans-serif, system-ui, sans-serif;
    font-weight: 900;
    letter-spacing: 0;
    text-transform: uppercase;
  }

  .pilgrim-hero h1 {
    max-width: min(100%, 760px);
    color: white;
    font-size: clamp(2.85rem, 4.7vw, 4.35rem);
    line-height: 1.01;
    text-transform: none;
  }

  .pilgrim-hero h1::after {
    content: "";
    display: block;
    width: min(190px, 38vw);
    height: 3px;
    margin-top: 22px;
    background: linear-gradient(90deg, var(--pilgrim-teal), rgba(255, 255, 255, 0));
  }

  .pilgrim-script {
    margin: 28px 0 0;
    color: white;
    font-family: "Pilgrim Summer", "Brush Script MT", cursive;
    font-size: clamp(3.4rem, 8vw, 6.8rem);
    line-height: 0.76;
    text-shadow: 0 4px 0 rgba(255, 255, 255, 0.06);
  }

  .pilgrim-script--navy {
    color: var(--pilgrim-navy);
  }

  .pilgrim-hero__lead {
    max-width: min(100%, 660px);
    margin: 34px 0 0;
    color: rgba(255, 255, 255, 0.82);
    font-size: clamp(1.05rem, 2vw, 1.28rem);
    line-height: 1.7;
  }

  .pilgrim-actions {
    display: flex;
    flex-wrap: wrap;
    gap: 14px;
    margin-top: 34px;
  }

  .pilgrim-button {
    display: inline-flex;
    min-height: 52px;
    align-items: center;
    justify-content: center;
    padding: 0 26px;
    border-radius: 999px;
    font-size: 0.82rem;
    font-weight: 900;
    letter-spacing: 0.16em;
    text-decoration: none;
    text-transform: uppercase;
    transition: transform 180ms ease, background 180ms ease, color 180ms ease;
  }

  .pilgrim-button:hover {
    transform: translateY(-2px);
  }

  .pilgrim-button--teal {
    border: 1px solid rgba(8, 174, 190, 0.95);
    background: var(--pilgrim-teal);
    color: var(--pilgrim-deep);
  }

  .pilgrim-button--line {
    border: 1px solid rgba(255, 255, 255, 0.62);
    background: rgba(7, 17, 31, 0.42);
    color: white;
    box-shadow: inset 0 0 0 1px rgba(255, 255, 255, 0.08);
  }

  .pilgrim-button--navy {
    border: 1px solid rgba(23, 36, 60, 0.95);
    background: var(--pilgrim-navy);
    color: white;
  }

  :global(html[data-cumbre-panel-theme='light'] #cumbre-back-link) {
    border-color: rgba(7, 17, 31, 0.2);
    background: rgba(255, 255, 255, 0.92);
    color: #07111c !important;
    box-shadow: 0 18px 48px rgba(7, 17, 31, 0.16);
  }

  :global(html[data-cumbre-panel-theme='light'] #cumbre-back-link:hover),
  :global(html[data-cumbre-panel-theme='light'] #cumbre-back-link:focus-visible) {
    border-color: rgba(7, 17, 31, 0.28);
    background: #ffffff;
    color: #07111c !important;
  }

  .pilgrim-button--navy-line {
    border: 1px solid rgba(255, 255, 255, 0.68);
    background: rgba(255, 255, 255, 0.86);
    color: var(--pilgrim-deep);
  }

  .pilgrim-hero__media {
    position: relative;
    display: grid;
    min-width: 0;
    justify-items: end;
    isolation: isolate;
  }

  .pilgrim-video-card {
    position: relative;
    z-index: 2;
    width: min(100%, 560px);
    margin: 0;
    overflow: hidden;
    padding: 12px;
    border: 1px solid rgba(255, 255, 255, 0.22);
    border-radius: 38px 38px 10px 38px;
    background:
      linear-gradient(145deg, rgba(255, 255, 255, 0.16), rgba(255, 255, 255, 0.055)),
      rgba(7, 17, 31, 0.72);
    box-shadow: 0 42px 110px rgba(0, 0, 0, 0.38);
    backdrop-filter: blur(18px);
  }

  .pilgrim-video-card::before {
    content: "";
    position: absolute;
    right: -76px;
    top: -84px;
    z-index: 0;
    width: 210px;
    height: 210px;
    border: 1px solid rgba(8, 174, 190, 0.28);
    border-radius: 999px;
    background: rgba(8, 174, 190, 0.12);
    pointer-events: none;
  }

  .pilgrim-video-card__topline {
    position: relative;
    z-index: 2;
    display: flex;
    min-height: 42px;
    align-items: center;
    justify-content: space-between;
    gap: 14px;
    padding: 0 10px 10px;
    color: rgba(255, 255, 255, 0.86);
    font-size: 0.68rem;
    font-weight: 950;
    letter-spacing: 0.18em;
    text-transform: uppercase;
  }

  .pilgrim-video-card__topline strong {
    display: inline-grid;
    min-height: 30px;
    place-items: center;
    border-radius: 999px;
    background: var(--pilgrim-teal);
    color: var(--pilgrim-deep);
    padding: 0 12px;
    font-size: 0.64rem;
    letter-spacing: 0.14em;
  }

  .pilgrim-video-frame {
    position: relative;
    z-index: 1;
    aspect-ratio: 16 / 9;
    overflow: hidden;
    border: 1px solid rgba(255, 255, 255, 0.18);
    border-radius: 28px 28px 8px 28px;
    background: #000;
  }

  .pilgrim-video-frame iframe {
    display: block;
    width: 100%;
    height: 100%;
    border: 0;
  }

  .pilgrim-video-card p {
    position: relative;
    z-index: 2;
    max-width: 29rem;
    margin: 12px 10px 4px;
    color: rgba(255, 255, 255, 0.76);
    font-size: 0.92rem;
    line-height: 1.42;
  }

  .pilgrim-poster {
    position: relative;
    width: 100%;
    max-width: 520px;
    aspect-ratio: 1;
    justify-self: end;
    overflow: hidden;
    border: 1px solid rgba(255, 255, 255, 0.2);
    border-radius: 0;
    box-shadow: 0 36px 90px rgba(0, 0, 0, 0.32);
  }

  .pilgrim-poster::after {
    content: "";
    position: absolute;
    inset: 18px;
    border: 1px solid rgba(255, 255, 255, 0.42);
    pointer-events: none;
  }

  .pilgrim-poster img,
  .pilgrim-flights img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .pilgrim-poster--stamp {
    position: absolute;
    right: -28px;
    bottom: -48px;
    z-index: 1;
    width: min(28vw, 168px);
    border-radius: 30px 30px 8px 30px;
    opacity: 0.92;
    transform: rotate(4deg);
  }

  .pilgrim-poster--stamp::after {
    inset: 10px;
  }

  .pilgrim-facts {
    display: grid;
    grid-template-columns: repeat(4, minmax(0, 1fr));
    gap: 1px;
    background: rgba(255, 255, 255, 0.18);
    border: 1px solid rgba(255, 255, 255, 0.18);
  }

  .pilgrim-facts--panel {
    grid-template-columns: repeat(2, minmax(0, 1fr));
    margin-top: 26px;
  }

  .pilgrim-facts article {
    min-height: 132px;
    display: grid;
    align-content: center;
    justify-items: start;
    padding: 24px;
    background: var(--pilgrim-navy);
  }

  .pilgrim-facts--panel article {
    min-height: 86px;
    padding: 16px;
  }

  .pilgrim-fact-icon {
    display: grid;
    width: 42px;
    height: 42px;
    margin-bottom: 16px;
    place-items: center;
    border: 1px solid rgba(8, 174, 190, 0.42);
    border-radius: 999px;
    color: var(--pilgrim-teal);
    font-size: 1.28rem;
  }

  .pilgrim-fact-label,
  .pilgrim-hotels__list span {
    color: var(--pilgrim-teal);
    font-size: 0.72rem;
    font-weight: 900;
    letter-spacing: 0.18em;
    text-transform: uppercase;
  }

  .pilgrim-facts strong {
    margin-top: 10px;
    color: rgba(255, 255, 255, 0.92);
    font-size: clamp(1.05rem, 2vw, 1.45rem);
    line-height: 1.15;
  }

  .pilgrim-route {
    padding: 96px 0;
    border-block: 1px solid rgba(255, 255, 255, 0.12);
    background:
      radial-gradient(circle at 18% 20%, rgba(8, 174, 190, 0.16), transparent 32%),
      linear-gradient(180deg, #101c31, var(--pilgrim-navy));
    color: white;
  }

  .pilgrim-route__grid {
    display: grid;
    grid-template-columns: minmax(360px, 0.95fr) minmax(0, 1fr);
    gap: 60px;
    align-items: center;
  }

  .pilgrim-route__map {
    position: relative;
    overflow: hidden;
    border: 1px solid rgba(255, 255, 255, 0.2);
    background: var(--pilgrim-teal);
    box-shadow: 0 28px 80px rgba(0, 0, 0, 0.24);
  }

  .pilgrim-route__map img {
    display: block;
    width: 100%;
    height: auto;
  }

  .pilgrim-route__content h2,
  .pilgrim-hotels h2,
  .pilgrim-final h2 {
    color: var(--pilgrim-navy);
    font-size: clamp(2.2rem, 4.8vw, 4.35rem);
    line-height: 1;
  }

  .pilgrim-route__content h2 {
    color: white;
  }

  .pilgrim-hotels h2 {
    color: white;
  }

  .pilgrim-display-title {
    display: grid;
    gap: 0;
    max-width: min(100%, 680px);
    margin: 0;
    font-family: "Intro", ui-sans-serif, system-ui, sans-serif;
    line-height: 0.9;
  }

  .pilgrim-display-title__outline,
  .pilgrim-display-title__solid {
    display: block;
    font-weight: 950;
    letter-spacing: 0;
    text-transform: uppercase;
  }

  .pilgrim-display-title__outline {
    color: transparent;
    -webkit-text-stroke: 1.4px rgba(255, 255, 255, 0.82);
    text-stroke: 1.4px rgba(255, 255, 255, 0.82);
    font-size: clamp(3.25rem, 6.2vw, 6.25rem);
    line-height: 0.82;
  }

  .pilgrim-display-title__solid {
    color: white;
    font-size: clamp(2.45rem, 4.85vw, 4.9rem);
    line-height: 0.86;
  }

  .pilgrim-display-title__script {
    display: block;
    margin-top: 0.08em;
    color: var(--pilgrim-teal);
    font-family: "Pilgrim Summer", "Brush Script MT", cursive;
    font-size: clamp(2.9rem, 5.4vw, 5.3rem);
    font-weight: 400;
    line-height: 0.72;
    text-transform: none;
  }

  .pilgrim-display-title--itinerary,
  .pilgrim-display-title--includes,
  .pilgrim-display-title--flights,
  .pilgrim-display-title--detail,
  .pilgrim-display-title--final {
    max-width: min(100%, 720px);
  }

  .pilgrim-display-title--itinerary .pilgrim-display-title__outline,
  .pilgrim-display-title--includes .pilgrim-display-title__outline,
  .pilgrim-display-title--flights .pilgrim-display-title__outline,
  .pilgrim-display-title--detail .pilgrim-display-title__outline {
    font-size: clamp(3rem, 6vw, 5.8rem);
  }

  .pilgrim-display-title--itinerary .pilgrim-display-title__solid,
  .pilgrim-display-title--includes .pilgrim-display-title__solid,
  .pilgrim-display-title--flights .pilgrim-display-title__solid,
  .pilgrim-display-title--detail .pilgrim-display-title__solid {
    font-size: clamp(2.35rem, 4.7vw, 4.45rem);
  }

  .pilgrim-display-title--detail {
    max-width: 620px;
  }

  .pilgrim-display-title--detail .pilgrim-display-title__outline {
    font-size: clamp(2.7rem, 5vw, 4.65rem);
  }

  .pilgrim-display-title--detail .pilgrim-display-title__solid {
    font-size: clamp(2.15rem, 4vw, 3.65rem);
  }

  .pilgrim-display-title--center {
    margin-inline: auto;
    justify-items: center;
  }

  .pilgrim-display-title--final .pilgrim-display-title__outline {
    -webkit-text-stroke-color: rgba(23, 36, 60, 0.72);
    text-stroke-color: rgba(23, 36, 60, 0.72);
    font-size: clamp(2.9rem, 5.4vw, 5.15rem);
  }

  .pilgrim-display-title--final .pilgrim-display-title__solid {
    color: var(--pilgrim-navy);
    font-size: clamp(2.35rem, 4.4vw, 4.05rem);
  }

  .pilgrim-display-title--final .pilgrim-display-title__script {
    color: white;
  }

  .pilgrim-display-title--hotels .pilgrim-display-title__outline {
    font-size: clamp(2.95rem, 5.6vw, 5.55rem);
  }

  .pilgrim-display-title--hotels .pilgrim-display-title__solid {
    font-size: clamp(2.3rem, 4.5vw, 4.5rem);
  }

  .pilgrim-route__content p,
  .pilgrim-hotels p,
  .pilgrim-final p {
    max-width: 620px;
    color: rgba(23, 36, 60, 0.82);
    font-size: 1.08rem;
    line-height: 1.72;
  }

  .pilgrim-route__content p {
    color: rgba(255, 255, 255, 0.78);
  }

  .pilgrim-hotels p {
    color: rgba(255, 255, 255, 0.76);
  }

  .pilgrim-route__stops {
    display: flex;
    flex-wrap: wrap;
    gap: 10px;
    margin-top: 28px;
  }

  .pilgrim-route__stops span {
    display: inline-flex;
    min-height: 38px;
    align-items: center;
    padding: 0 14px;
    border: 1px solid rgba(8, 174, 190, 0.36);
    border-radius: 999px;
    background: rgba(8, 174, 190, 0.12);
    color: white;
    font-size: 0.86rem;
    font-weight: 900;
  }

  .pilgrim-pillars {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 18px;
    padding: 86px 0;
  }

  .pilgrim-pillars article {
    min-height: 250px;
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    padding: 28px;
    border: 1px solid rgba(255, 255, 255, 0.14);
    background: rgba(255, 255, 255, 0.04);
  }

  .pilgrim-pillars span {
    color: var(--pilgrim-teal);
    font-size: 0.82rem;
    font-weight: 900;
    letter-spacing: 0.16em;
  }

  .pilgrim-pillars h3 {
    margin: 42px 0 12px;
    color: white;
    font-size: 1.7rem;
    line-height: 1;
  }

  .pilgrim-pillars p {
    margin: 0;
    color: rgba(255, 255, 255, 0.74);
    line-height: 1.6;
  }

  .pilgrim-section {
    padding: 96px 0;
  }

  .pilgrim-section--navy {
    background: var(--pilgrim-navy);
    color: white;
  }

  .pilgrim-section-head {
    max-width: 850px;
    margin-bottom: 46px;
  }

  .pilgrim-section-head--story {
    max-width: 620px;
    margin-bottom: 0;
  }

  .pilgrim-section-head--center {
    max-width: none;
    text-align: center;
  }

  .pilgrim-section-head h2,
  .pilgrim-flights h2 {
    color: white;
    font-size: clamp(2.6rem, 6vw, 5.5rem);
    line-height: 0.95;
  }

  .pilgrim-section-head--center h2 {
    color: var(--pilgrim-teal);
  }

  .pilgrim-section-head p:not(.pilgrim-kicker):not(.pilgrim-script) {
    max-width: 680px;
    color: rgba(255, 255, 255, 0.72);
    font-size: 1.05rem;
    line-height: 1.7;
  }

  .pilgrim-itinerary__grid,
  .pilgrim-includes-panel__grid {
    display: grid;
    grid-template-columns: minmax(0, 0.74fr) minmax(460px, 1fr);
    gap: clamp(32px, 5vw, 72px);
    align-items: center;
  }

  .pilgrim-itinerary__side,
  .pilgrim-includes-panel__side {
    min-width: 0;
  }

  .pilgrim-days {
    display: grid;
    gap: 1px;
    border-block: 1px solid rgba(255, 255, 255, 0.42);
  }

  .pilgrim-days--preview {
    max-height: min(46svh, 410px);
    overflow: hidden;
  }

  .pilgrim-day {
    display: grid;
    grid-template-columns: 110px minmax(0, 1fr) 150px;
    gap: 28px;
    align-items: center;
    padding: 28px 0;
    border-bottom: 1px solid rgba(255, 255, 255, 0.42);
  }

  .pilgrim-day--preview {
    grid-template-columns: 66px minmax(0, 1fr) 96px;
    gap: 16px;
    padding: 12px 0;
  }

  .pilgrim-day--preview .pilgrim-day__badge {
    width: 58px;
    height: 58px;
  }

  .pilgrim-day--preview .pilgrim-day__badge strong {
    font-size: 1.35rem;
  }

  .pilgrim-day--preview .pilgrim-day__body h3 {
    font-size: clamp(1.1rem, 2.1vw, 1.65rem);
    line-height: 1.05;
  }

  .pilgrim-day--preview .pilgrim-day__body p {
    margin-bottom: 4px;
    font-size: 0.78rem;
  }

  .pilgrim-day--preview .pilgrim-day__date {
    padding-left: 16px;
    font-size: 1.05rem;
  }

  .pilgrim-day:last-child {
    border-bottom: 0;
  }

  .pilgrim-day__badge {
    aspect-ratio: 1;
    display: grid;
    place-items: center;
    border-radius: 999px;
    background: var(--pilgrim-teal);
    color: var(--pilgrim-navy);
  }

  .pilgrim-day__badge span {
    align-self: end;
    font-size: 0.68rem;
    font-weight: 900;
    letter-spacing: 0.12em;
    text-transform: uppercase;
  }

  .pilgrim-day__badge strong {
    align-self: start;
    font-size: 2.1rem;
    line-height: 0.9;
  }

  .pilgrim-day__body p {
    margin: 0 0 8px;
    color: rgba(255, 255, 255, 0.74);
    font-size: 0.92rem;
  }

  .pilgrim-day__body h3 {
    margin: 0;
    color: white;
    font-size: clamp(1.7rem, 3.5vw, 3.2rem);
    line-height: 0.95;
  }

  .pilgrim-day__body small {
    display: block;
    max-width: 720px;
    margin-top: 12px;
    color: rgba(255, 255, 255, 0.72);
    font-size: 1rem;
    line-height: 1.55;
  }

  .pilgrim-day__date {
    padding-left: 28px;
    border-left: 3px solid var(--pilgrim-teal);
    color: white;
    font-size: clamp(1.4rem, 2.6vw, 2.1rem);
    font-weight: 900;
    line-height: 1.05;
  }

  .pilgrim-hotels {
    padding: 88px 0;
    border-block: 1px solid rgba(255, 255, 255, 0.12);
    background:
      radial-gradient(circle at 78% 16%, rgba(8, 174, 190, 0.18), transparent 30%),
      linear-gradient(180deg, var(--pilgrim-navy), #101c31);
    color: white;
  }

  .pilgrim-hotels__grid {
    display: grid;
    grid-template-columns: minmax(0, 0.7fr) minmax(0, 1fr);
    gap: 48px;
    align-items: start;
  }

  .pilgrim-hotels__list {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 12px;
  }

  .pilgrim-hotels__list article {
    min-height: 150px;
    padding: 22px;
    background: rgba(255, 255, 255, 0.94);
    border: 1px solid rgba(255, 255, 255, 0.22);
    box-shadow: 0 20px 56px rgba(0, 0, 0, 0.16);
  }

  .pilgrim-hotels__list span {
    color: #087f8b;
  }

  .pilgrim-hotels__list strong {
    display: block;
    margin-top: 12px;
    color: var(--pilgrim-deep);
    font-size: 1rem;
    line-height: 1.45;
  }

  .pilgrim-overview-grid {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 16px;
  }

  .pilgrim-overview-grid article {
    min-height: 250px;
    padding: 24px;
    border: 1px solid rgba(255, 255, 255, 0.16);
    background:
      linear-gradient(145deg, rgba(255, 255, 255, 0.12), rgba(255, 255, 255, 0.045)),
      rgba(255, 255, 255, 0.055);
  }

  .pilgrim-overview-icon {
    display: grid;
    width: 58px;
    height: 58px;
    margin-bottom: 24px;
    place-items: center;
    border: 1px solid rgba(8, 174, 190, 0.42);
    border-radius: 999px;
    color: var(--pilgrim-teal);
    font-size: 1.65rem;
  }

  .pilgrim-overview-label {
    color: var(--pilgrim-teal);
    font-size: 0.72rem;
    font-weight: 900;
    letter-spacing: 0.18em;
    text-transform: uppercase;
  }

  .pilgrim-overview-grid ul {
    display: grid;
    gap: 12px;
    margin: 18px 0 0;
    padding: 0;
    list-style: none;
  }

  .pilgrim-overview-grid li {
    color: rgba(255, 255, 255, 0.78);
    font-size: 0.92rem;
    line-height: 1.42;
  }

  .pilgrim-includes {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 18px;
  }

  .pilgrim-includes article {
    padding: 30px;
    border: 1px solid rgba(255, 255, 255, 0.14);
    background: rgba(255, 255, 255, 0.04);
  }

  .pilgrim-includes h3 {
    margin: 0 0 20px;
    color: white;
    font-size: 1.55rem;
  }

  .pilgrim-includes ul {
    display: grid;
    gap: 14px;
    margin: 0;
    padding: 0;
    list-style: none;
  }

  .pilgrim-includes li {
    position: relative;
    padding-left: 36px;
    color: rgba(255, 255, 255, 0.78);
    line-height: 1.55;
  }

  .pilgrim-includes li::before {
    content: "";
    position: absolute;
    left: 0;
    top: 0.2em;
    width: 22px;
    height: 22px;
    border-radius: 999px;
    background: var(--pilgrim-teal);
  }

  .pilgrim-includes li::after {
    content: "";
    position: absolute;
    left: 7px;
    top: 0.52em;
    width: 8px;
    height: 4px;
    border-left: 2px solid var(--pilgrim-navy);
    border-bottom: 2px solid var(--pilgrim-navy);
    transform: rotate(-45deg);
  }

  .pilgrim-includes__not li::before {
    background: #d93057;
  }

  .pilgrim-includes__not li::after {
    left: 6px;
    top: 0.42em;
    width: 10px;
    height: 10px;
    border: 0;
    background:
      linear-gradient(45deg, transparent 44%, var(--pilgrim-navy) 45%, var(--pilgrim-navy) 55%, transparent 56%),
      linear-gradient(-45deg, transparent 44%, var(--pilgrim-navy) 45%, var(--pilgrim-navy) 55%, transparent 56%);
    transform: none;
  }

  .pilgrim-flights {
    position: relative;
    min-height: 640px;
    display: grid;
    align-items: center;
    overflow: hidden;
    background: var(--pilgrim-deep);
  }

  @media (min-width: 981px) {
    .pilgrim-facts,
    .pilgrim-pillars {
      min-height: calc(100svh - 68px);
      align-content: center;
    }

    .pilgrim-facts--panel {
      min-height: 0;
      align-content: stretch;
    }

    .pilgrim-route,
    .pilgrim-hotels,
    .pilgrim-flights,
    .pilgrim-final {
      min-height: 100svh;
      display: grid;
      align-items: center;
    }

    .pilgrim-section--navy:not(#itinerario):not(#incluye) {
      min-height: 100svh;
      display: grid;
      align-items: center;
    }
  }

  @media (min-width: 981px) and (max-width: 1240px) {
    .pilgrim-route__grid {
      gap: 38px;
    }

    .pilgrim-route__content h2 {
      font-size: clamp(2.1rem, 4.2vw, 3.2rem);
    }

    .pilgrim-facts--panel {
      margin-top: 18px;
    }

    .pilgrim-facts--panel article {
      min-height: 78px;
      padding: 14px;
    }

    .pilgrim-route__stops {
      gap: 8px;
      margin-top: 18px;
    }

    .pilgrim-route__stops span {
      min-height: 34px;
      padding: 0 12px;
      font-size: 0.8rem;
    }
  }

  @media (min-width: 981px) and (max-width: 1100px) {
    .pilgrim-itinerary__grid,
    .pilgrim-includes-panel__grid {
      grid-template-columns: minmax(0, 0.62fr) minmax(420px, 1fr);
      gap: 28px;
    }

    .pilgrim-display-title--includes .pilgrim-display-title__outline,
    .pilgrim-display-title--itinerary .pilgrim-display-title__outline {
      font-size: clamp(2.65rem, 5.4vw, 4.2rem);
    }

    .pilgrim-display-title--includes .pilgrim-display-title__solid,
    .pilgrim-display-title--itinerary .pilgrim-display-title__solid {
      font-size: clamp(2.05rem, 4.3vw, 3.35rem);
    }

    .pilgrim-display-title--includes .pilgrim-display-title__script,
    .pilgrim-display-title--itinerary .pilgrim-display-title__script {
      font-size: clamp(2.5rem, 5vw, 3.6rem);
    }

    .pilgrim-overview-grid {
      gap: 10px;
    }

    .pilgrim-overview-grid article {
      min-height: 210px;
      padding: 18px;
    }

    .pilgrim-overview-icon {
      width: 46px;
      height: 46px;
      margin-bottom: 16px;
      font-size: 1.32rem;
    }

    .pilgrim-overview-grid ul {
      gap: 9px;
      margin-top: 14px;
    }

    .pilgrim-overview-grid li {
      font-size: 0.82rem;
      line-height: 1.32;
    }
  }

  @media (min-width: 981px) and (max-height: 800px) {
    .pilgrim-panel-inner {
      padding: 58px 0 42px;
    }

    .pilgrim-hero__grid {
      grid-template-columns: minmax(0, 0.98fr) minmax(360px, 0.72fr);
      gap: 34px;
    }

    .pilgrim-video-card {
      width: min(100%, 500px);
      padding: 10px;
    }

    .pilgrim-video-card__topline {
      min-height: 36px;
      padding-bottom: 8px;
    }

    .pilgrim-video-card p {
      margin-top: 10px;
      font-size: 0.82rem;
      line-height: 1.34;
    }

    .pilgrim-route__grid {
      align-items: center;
    }

    .pilgrim-route__map {
      max-width: 520px;
      justify-self: start;
    }

    .pilgrim-display-title__outline {
      font-size: clamp(2.75rem, 5vw, 4.6rem);
    }

    .pilgrim-display-title__solid {
      font-size: clamp(2.1rem, 4vw, 3.75rem);
    }

    .pilgrim-display-title__script {
      font-size: clamp(2.45rem, 4.6vw, 4rem);
    }

    .pilgrim-route__content p,
    .pilgrim-hotels p,
    .pilgrim-final p,
    .pilgrim-section-head p:not(.pilgrim-kicker):not(.pilgrim-script) {
      font-size: 0.96rem;
      line-height: 1.48;
    }

    .pilgrim-facts--panel {
      margin-top: 16px;
    }

    .pilgrim-facts--panel article {
      min-height: 74px;
      padding: 12px;
    }

    .pilgrim-fact-icon {
      width: 30px;
      height: 30px;
      margin-bottom: 8px;
      font-size: 0.95rem;
    }

    .pilgrim-facts strong {
      margin-top: 5px;
      font-size: 0.95rem;
    }

    .pilgrim-route__stops {
      display: none;
    }

    .pilgrim-section-head {
      margin-bottom: 28px;
    }

    .pilgrim-days--preview {
      max-height: min(42svh, 340px);
    }

    .pilgrim-day--preview {
      padding: 8px 0;
    }

    .pilgrim-hotels__list article {
      min-height: 118px;
      padding: 18px;
    }

    .pilgrim-overview-grid article {
      min-height: 220px;
      padding: 18px;
    }

    .pilgrim-overview-icon {
      width: 44px;
      height: 44px;
      margin-bottom: 14px;
      font-size: 1.25rem;
    }

    .pilgrim-overview-grid li {
      font-size: 0.82rem;
      line-height: 1.32;
    }

    .pilgrim-final__box {
      gap: 34px;
    }
  }

  @media (min-width: 981px) and (max-height: 700px) {
    .pilgrim-video-card {
      width: min(100%, 450px);
    }

    .pilgrim-video-card p {
      display: none;
    }

    .pilgrim-days--preview {
      max-height: none;
    }

    .pilgrim-days--preview .pilgrim-day--preview:nth-child(n+4) {
      display: none;
    }
  }

  .pilgrim-flights img,
  .pilgrim-flights__shade {
    position: absolute;
    inset: 0;
  }

  .pilgrim-flights__shade {
    background:
      linear-gradient(90deg, rgba(7, 17, 31, 0.97) 0%, rgba(23, 36, 60, 0.91) 46%, rgba(23, 36, 60, 0.74) 100%),
      linear-gradient(0deg, rgba(7, 17, 31, 0.94), rgba(7, 17, 31, 0.12));
  }

  .pilgrim-flights__content {
    position: relative;
    z-index: 1;
    display: grid;
    grid-template-columns: minmax(0, 0.72fr) minmax(460px, 1fr);
    gap: clamp(34px, 6vw, 84px);
    align-items: center;
    padding: 96px 0;
  }

  .pilgrim-flights__content h2 {
    max-width: 780px;
  }

  .pilgrim-flight-notes {
    display: grid;
    gap: 12px;
    margin-top: 0;
  }

  .pilgrim-flight-notes p {
    margin: 0;
    padding: 18px 20px 18px 24px;
    border-left: 4px solid var(--pilgrim-teal);
    background: rgba(255, 255, 255, 0.1);
    color: rgba(255, 255, 255, 0.84);
    line-height: 1.6;
  }

  .pilgrim-care {
    display: grid;
    gap: 1px;
    border-block: 1px solid rgba(255, 255, 255, 0.38);
  }

  .pilgrim-care article {
    display: grid;
    grid-template-columns: 74px minmax(0, 1fr);
    gap: 28px;
    align-items: start;
    padding: 34px 0;
    border-bottom: 1px solid rgba(255, 255, 255, 0.38);
  }

  .pilgrim-care article:last-child {
    border-bottom: 0;
  }

  .pilgrim-care__icon {
    display: grid;
    width: 74px;
    height: 74px;
    place-items: center;
    border: 1px solid rgba(8, 174, 190, 0.48);
    border-radius: 26px 26px 6px 26px;
    background: var(--pilgrim-teal);
    color: var(--pilgrim-navy);
    font-size: 2.15rem;
    box-shadow: 0 24px 60px rgba(0, 0, 0, 0.22);
  }

  .pilgrim-care h3 {
    margin: 0;
    color: white;
    font-size: 1.45rem;
    text-transform: uppercase;
  }

  .pilgrim-care p {
    max-width: 820px;
    margin: 8px 0 0;
    color: rgba(255, 255, 255, 0.78);
    font-size: 1.15rem;
    line-height: 1.6;
  }

  .pilgrim-final {
    padding: 82px 0 112px;
    background: var(--pilgrim-teal);
    color: var(--pilgrim-navy);
  }

  .pilgrim-final__box {
    display: flex;
    gap: 28px;
    align-items: center;
    justify-content: space-between;
  }

  .pilgrim-final p {
    max-width: 640px;
  }

  .pilgrim-final__actions {
    display: flex;
    flex-wrap: wrap;
    justify-content: flex-end;
    gap: 12px;
  }

  .pilgrim-detail-section {
    scroll-margin-top: 72px;
  }

  .pilgrim-mobile-cta {
    display: none;
  }

  @media (max-width: 980px) {
    .pilgrim-hero__grid,
    .pilgrim-route__grid,
    .pilgrim-hotels__grid,
    .pilgrim-itinerary__grid,
    .pilgrim-includes-panel__grid,
    .pilgrim-flights__content {
      grid-template-columns: 1fr;
      gap: 34px;
    }

    .pilgrim-section-head--story {
      max-width: 100%;
    }

    .pilgrim-hero__media {
      justify-items: center;
    }

    .pilgrim-video-card {
      width: min(100%, 560px);
    }

    .pilgrim-poster {
      max-width: 620px;
      justify-self: center;
    }

    .pilgrim-facts,
    .pilgrim-pillars,
    .pilgrim-includes,
    .pilgrim-overview-grid,
    .pilgrim-hotels__list {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }
  }

  @media (max-width: 680px) {
    :global(.cumbre-action--whatsapp) {
      display: none !important;
    }

    .pilgrim-shell {
      width: min(100% - 24px, 1160px);
    }

    .pilgrim-hero {
      min-height: auto;
      padding: 96px 0 46px;
    }

    .pilgrim-hero__grid {
      gap: 34px;
    }

    .pilgrim-hero h1 {
      max-width: 100%;
      font-size: clamp(1.95rem, 8vw, 2.65rem);
      line-height: 1;
    }

    .pilgrim-hero h1 strong {
      font-size: 0.52em;
    }

    .pilgrim-script {
      font-size: clamp(2.6rem, 13vw, 4rem);
    }

    .pilgrim-hero__lead {
      max-width: 22rem;
      font-size: 1rem;
      line-height: 1.55;
    }

    .pilgrim-actions {
      display: flex;
      flex-direction: column;
      align-items: center;
      margin-top: 28px;
    }

    .pilgrim-actions .pilgrim-button {
      width: min(100%, 360px);
    }

    .pilgrim-video-card {
      padding: 10px;
      border-radius: 30px 30px 8px 30px;
    }

    .pilgrim-video-card__topline {
      min-height: 34px;
      padding: 0 6px 8px;
      font-size: 0.58rem;
      letter-spacing: 0.15em;
    }

    .pilgrim-video-card p {
      display: none;
    }

    .pilgrim-video-frame {
      border-radius: 22px 22px 7px 22px;
    }

    .pilgrim-poster::after {
      inset: 12px;
    }

    .pilgrim-poster {
      max-width: 100%;
    }

    .pilgrim-poster--stamp {
      display: none;
    }

    .pilgrim-facts,
    .pilgrim-pillars,
    .pilgrim-includes,
    .pilgrim-overview-grid,
    .pilgrim-hotels__list {
      grid-template-columns: 1fr;
    }

    .pilgrim-facts article {
      min-height: 104px;
      padding: 22px;
    }

    .pilgrim-route,
    .pilgrim-section,
    .pilgrim-hotels {
      padding: 52px 0 64px;
    }

    .pilgrim-route__content h2,
    .pilgrim-section-head h2,
    .pilgrim-hotels h2,
    .pilgrim-flights h2,
    .pilgrim-final h2 {
      font-size: clamp(1.9rem, 8.6vw, 2.8rem);
      line-height: 1.03;
    }

    .pilgrim-pillars {
      padding: 64px 0;
    }

    .pilgrim-pillars article {
      min-height: 200px;
    }

    .pilgrim-day {
      grid-template-columns: 76px minmax(0, 1fr);
      gap: 16px;
      padding: 24px 0;
    }

    .pilgrim-day__badge {
      width: 76px;
      height: 76px;
    }

    .pilgrim-day__badge strong {
      font-size: 1.55rem;
    }

    .pilgrim-day__date {
      grid-column: 2;
      padding-left: 14px;
      font-size: 1.1rem;
    }

    .pilgrim-day__body h3 {
      font-size: clamp(1.5rem, 8vw, 2.2rem);
    }

    .pilgrim-day__body small {
      font-size: 0.95rem;
    }

    .pilgrim-flights {
      min-height: auto;
    }

    .pilgrim-flights__shade {
      background: rgba(7, 17, 31, 0.88);
    }

    .pilgrim-flights__content {
      grid-template-columns: 1fr;
      gap: 28px;
      padding: 72px 0;
    }

    .pilgrim-care article {
      grid-template-columns: 48px minmax(0, 1fr);
      gap: 18px;
    }

    .pilgrim-care__icon {
      width: 44px;
      height: 44px;
      border-radius: 16px 16px 4px 16px;
      font-size: 1.3rem;
    }

    .pilgrim-care p {
      font-size: 1rem;
    }

    .pilgrim-final {
      padding: 70px 0 112px;
    }

    .pilgrim-final__box {
      display: block;
    }

    .pilgrim-final__actions {
      justify-content: stretch;
      margin-top: 24px;
    }

    .pilgrim-final__actions .pilgrim-button {
      width: 100%;
    }

    .pilgrim-mobile-cta {
      position: fixed;
      left: 12px;
      right: 12px;
      bottom: 12px;
      z-index: 60;
      display: none;
    }

    .pilgrim-mobile-cta a {
      display: flex;
      min-height: 54px;
      align-items: center;
      justify-content: center;
      border-radius: 999px;
      background: var(--pilgrim-teal);
      color: var(--pilgrim-deep);
      box-shadow: 0 20px 50px rgba(0, 0, 0, 0.28);
      font-size: 0.82rem;
      font-weight: 950;
      letter-spacing: 0.14em;
      text-decoration: none;
      text-transform: uppercase;
    }
  }

  @media (min-width: 520px) and (max-width: 980px) {
    .pilgrim-panel-inner {
      padding: 70px 0 46px;
    }

    .pilgrim-hero__grid,
    .pilgrim-route__grid,
    .pilgrim-hotels__grid,
    .pilgrim-itinerary__grid,
    .pilgrim-includes-panel__grid,
    .pilgrim-flights__content {
      grid-template-columns: minmax(0, 1fr);
      gap: 22px;
    }

    .pilgrim-hero h1 {
      font-size: clamp(2.15rem, 6.6vw, 3rem);
      line-height: 1.02;
    }

    .pilgrim-hero h1::after {
      width: 150px;
      margin-top: 16px;
    }

    .pilgrim-script {
      margin-top: 18px;
      font-size: clamp(2.7rem, 9vw, 4.2rem);
    }

    .pilgrim-hero__lead,
    .pilgrim-route__content p,
    .pilgrim-hotels p {
      max-width: 36rem;
      margin-top: 18px;
      font-size: 0.96rem;
      line-height: 1.45;
    }

    .pilgrim-actions {
      align-items: center;
      margin-top: 20px;
    }

    .pilgrim-button {
      min-height: 46px;
      padding: 0 18px;
      font-size: 0.72rem;
    }

    .pilgrim-hero__media {
      justify-items: center;
    }

    .pilgrim-video-card {
      width: min(100%, 430px);
      padding: 8px;
      border-radius: 26px 26px 8px 26px;
    }

    .pilgrim-video-card__topline {
      min-height: 30px;
      padding: 0 6px 7px;
    }

    .pilgrim-video-card p {
      display: none;
    }

    .pilgrim-video-frame {
      border-radius: 20px 20px 7px 20px;
    }

    .pilgrim-poster {
      display: none;
    }

    .pilgrim-route__map {
      max-width: 360px;
    }

    .pilgrim-route__content h2,
    .pilgrim-section-head h2,
    .pilgrim-hotels h2,
    .pilgrim-final h2 {
      font-size: clamp(1.95rem, 6.6vw, 3rem);
      line-height: 0.98;
    }

    .pilgrim-display-title {
      max-width: 36rem;
    }

    .pilgrim-display-title__outline {
      -webkit-text-stroke-width: 1.1px;
      text-stroke-width: 1.1px;
      font-size: clamp(2.45rem, 8vw, 3.85rem);
    }

    .pilgrim-display-title__solid {
      font-size: clamp(1.95rem, 6.8vw, 3.1rem);
    }

    .pilgrim-display-title__script {
      font-size: clamp(2.4rem, 8vw, 3.7rem);
    }

    .pilgrim-facts--panel {
      grid-template-columns: repeat(2, minmax(0, 1fr));
      margin-top: 16px;
    }

    .pilgrim-facts--panel article {
      min-height: 70px;
      padding: 12px;
    }

    .pilgrim-fact-icon {
      width: 30px;
      height: 30px;
      margin-bottom: 8px;
      font-size: 0.94rem;
    }

    .pilgrim-fact-label,
    .pilgrim-hotels__list span,
    .pilgrim-overview-label {
      font-size: 0.62rem;
      letter-spacing: 0.14em;
    }

    .pilgrim-facts strong {
      margin-top: 6px;
      font-size: 0.95rem;
    }

    .pilgrim-route__stops {
      display: none;
    }

    .pilgrim-section-head {
      margin-bottom: 24px;
    }

    .pilgrim-days--preview {
      max-height: 48svh;
    }

    .pilgrim-day--preview {
      grid-template-columns: 52px minmax(0, 1fr) 74px;
      gap: 12px;
      padding: 9px 0;
    }

    .pilgrim-day--preview .pilgrim-day__badge {
      width: 48px;
      height: 48px;
    }

    .pilgrim-day--preview .pilgrim-day__badge strong {
      font-size: 1.05rem;
    }

    .pilgrim-day--preview .pilgrim-day__body h3 {
      font-size: clamp(0.98rem, 2.7vw, 1.35rem);
    }

    .pilgrim-day--preview .pilgrim-day__date {
      grid-column: auto;
      padding-left: 10px;
      font-size: 0.84rem;
    }

    .pilgrim-hotels__list {
      grid-template-columns: repeat(2, minmax(0, 1fr));
      gap: 8px;
    }

    .pilgrim-hotels__list article {
      min-height: 82px;
      padding: 14px;
    }

    .pilgrim-hotels__list strong {
      margin-top: 8px;
      font-size: 0.82rem;
      line-height: 1.25;
    }

    .pilgrim-overview-grid {
      grid-template-columns: repeat(3, minmax(0, 1fr));
      gap: 8px;
    }

    .pilgrim-overview-grid article {
      min-height: 0;
      padding: 14px;
    }

    .pilgrim-overview-icon {
      width: 38px;
      height: 38px;
      margin-bottom: 12px;
      font-size: 1.1rem;
    }

    .pilgrim-overview-grid ul {
      gap: 8px;
      margin-top: 12px;
    }

    .pilgrim-overview-grid li {
      font-size: 0.76rem;
      line-height: 1.28;
    }

    .pilgrim-overview-grid li:nth-child(n+4) {
      display: none;
    }

    .pilgrim-final__box {
      display: grid;
      gap: 22px;
    }
  }

  @media (min-width: 520px) and (max-width: 700px) {
    .pilgrim-route__map {
      display: none;
    }

    .pilgrim-display-title__outline {
      font-size: clamp(2.2rem, 9vw, 3.15rem);
    }

    .pilgrim-display-title__solid {
      font-size: clamp(1.8rem, 7.4vw, 2.55rem);
    }

    .pilgrim-display-title__script {
      font-size: clamp(2.25rem, 9vw, 3.1rem);
    }

    .pilgrim-section-head p:not(.pilgrim-kicker):not(.pilgrim-script) {
      font-size: 0.9rem;
      line-height: 1.45;
    }

    .pilgrim-overview-grid {
      grid-template-columns: 1fr;
    }

    .pilgrim-overview-grid article {
      padding: 12px 14px;
    }

    .pilgrim-overview-grid li:nth-child(n+3) {
      display: none;
    }
  }

  @media (min-width: 981px) {
    .pilgrim-route__grid {
      grid-template-columns: minmax(0, 0.94fr) minmax(0, 1fr);
      grid-template-areas:
        "route-head route-head"
        "route-map route-facts"
        "route-stops route-stops";
      gap: 26px 52px;
      align-content: center;
      align-items: center;
    }

    .pilgrim-route__content {
      grid-area: route-head;
      display: grid;
      max-width: 920px;
      margin-inline: auto;
      justify-items: center;
      text-align: center;
    }

    .pilgrim-route__content p:not(.pilgrim-kicker) {
      max-width: 760px;
      margin: 14px auto 0;
    }

    .pilgrim-display-title--route {
      max-width: 850px;
      justify-items: center;
    }

    .pilgrim-route__map {
      grid-area: route-map;
      width: 100%;
      max-width: none;
      justify-self: stretch;
    }

    .pilgrim-route__grid > .pilgrim-facts--panel {
      grid-area: route-facts;
      height: 100%;
      margin-top: 0;
      align-self: stretch;
    }

    .pilgrim-route__grid > .pilgrim-facts--panel article {
      min-height: 120px;
      padding: 22px;
    }

    .pilgrim-route__stops {
      grid-area: route-stops;
      max-width: 980px;
      margin: 0 auto;
      justify-content: center;
    }

    .pilgrim-itinerary__grid,
    .pilgrim-includes-panel__grid {
      grid-template-columns: 1fr;
      gap: 28px;
      align-content: center;
    }

    .pilgrim-itinerary__grid .pilgrim-section-head--story,
    .pilgrim-includes-panel__grid .pilgrim-section-head--story {
      max-width: 780px;
      margin-inline: auto;
      text-align: center;
    }

    .pilgrim-itinerary__grid .pilgrim-section-head--story p:not(.pilgrim-kicker),
    .pilgrim-includes-panel__grid .pilgrim-section-head--story p:not(.pilgrim-kicker) {
      margin-inline: auto;
    }

    .pilgrim-display-title--itinerary,
    .pilgrim-display-title--includes {
      margin-inline: auto;
      justify-items: center;
    }

    .pilgrim-itinerary__side,
    .pilgrim-includes-panel__side {
      display: grid;
      justify-items: center;
      gap: 22px;
    }

    .pilgrim-days--preview {
      width: 100%;
      max-height: none;
      display: flex;
      gap: 12px;
      overflow-x: auto;
      overflow-y: hidden;
      border-block: 0;
      scroll-snap-type: x mandatory;
      scrollbar-width: none;
    }

    .pilgrim-days--preview::-webkit-scrollbar,
    .pilgrim-hotels__list::-webkit-scrollbar,
    .pilgrim-overview-grid::-webkit-scrollbar {
      display: none;
    }

    .pilgrim-day--preview {
      flex: 0 0 clamp(176px, 15.4vw, 232px);
      min-height: 176px;
      grid-template-columns: 1fr;
      align-content: start;
      gap: 12px;
      padding: 18px;
      border: 1px solid rgba(255, 255, 255, 0.22);
      background: rgba(255, 255, 255, 0.055);
      scroll-snap-align: start;
    }

    .pilgrim-day--preview .pilgrim-day__badge {
      width: 50px;
      height: 50px;
    }

    .pilgrim-day--preview .pilgrim-day__body h3 {
      font-size: clamp(1rem, 1.45vw, 1.25rem);
      line-height: 1.1;
    }

    .pilgrim-day--preview .pilgrim-day__date {
      padding-left: 12px;
      font-size: 0.92rem;
    }

    .pilgrim-hotels__grid {
      grid-template-columns: 1fr;
      gap: 34px;
      align-content: center;
      text-align: center;
    }

    .pilgrim-hotels__grid > div:first-child {
      display: grid;
      max-width: 760px;
      margin-inline: auto;
      justify-items: center;
    }

    .pilgrim-hotels__grid > div:first-child p {
      margin-inline: auto;
    }

    .pilgrim-display-title--hotels {
      margin-inline: auto;
      justify-items: center;
    }

    .pilgrim-hotels__list {
      display: flex;
      gap: 14px;
      overflow-x: auto;
      padding: 4px 0 12px;
      scroll-snap-type: x mandatory;
      scrollbar-width: none;
    }

    .pilgrim-hotels__list article {
      flex: 0 0 clamp(210px, 18vw, 290px);
      min-height: 168px;
      display: grid;
      align-content: center;
      text-align: left;
      scroll-snap-align: center;
    }

    .pilgrim-includes-panel__grid {
      gap: 30px;
    }

    .pilgrim-overview-grid {
      width: 100%;
      display: flex;
      gap: 14px;
      overflow-x: auto;
      padding-bottom: 10px;
      scroll-snap-type: x mandatory;
      scrollbar-width: none;
    }

    .pilgrim-overview-grid article {
      flex: 0 0 clamp(280px, 30vw, 360px);
      min-height: 292px;
      display: grid;
      align-content: start;
      scroll-snap-align: center;
    }

    .pilgrim-includes-panel__side .pilgrim-actions {
      margin-top: 0;
      justify-content: center;
    }

    .pilgrim-final__box {
      display: grid;
      grid-template-columns: minmax(0, 1fr);
      gap: 28px;
      justify-items: center;
      align-content: center;
      text-align: center;
    }

    .pilgrim-final__box > div:first-child {
      display: grid;
      justify-items: center;
    }

    .pilgrim-final p {
      margin-inline: auto;
    }

    .pilgrim-final__actions {
      justify-content: center;
      padding: 12px;
      border: 1px solid rgba(23, 36, 60, 0.16);
      border-radius: 999px;
      background: rgba(255, 255, 255, 0.16);
    }
  }

  .pilgrim-flights {
    min-height: auto;
  }

  .pilgrim-flights__content {
    grid-template-columns: 1fr;
    gap: 38px;
    align-content: center;
  }

  .pilgrim-flights__intro {
    max-width: 860px;
    margin-inline: auto;
    text-align: center;
  }

  .pilgrim-display-title--flights {
    margin-inline: auto;
    justify-items: center;
  }

  .pilgrim-flight-notes {
    counter-reset: flight-note;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 14px;
  }

  .pilgrim-flight-notes p {
    position: relative;
    min-height: 132px;
    padding: 22px 22px 22px 72px;
    border-left: 0;
    background: rgba(255, 255, 255, 0.12);
  }

  .pilgrim-flight-notes p::before {
    counter-increment: flight-note;
    content: counter(flight-note, decimal-leading-zero);
    position: absolute;
    left: 20px;
    top: 22px;
    display: grid;
    width: 34px;
    height: 34px;
    place-items: center;
    border-radius: 999px;
    background: var(--pilgrim-teal);
    color: var(--pilgrim-navy);
    font-size: 0.72rem;
    font-weight: 950;
  }

  @media (min-width: 1320px) {
    .pilgrim-flight-notes {
      grid-template-columns: repeat(3, minmax(0, 1fr));
    }
  }

  @media (min-width: 981px) and (max-height: 740px) {
    .pilgrim-route__grid {
      gap: 18px 42px;
    }

    .pilgrim-itinerary__grid,
    .pilgrim-includes-panel__grid {
      gap: 18px;
    }

    .pilgrim-display-title--route .pilgrim-display-title__outline {
      font-size: clamp(2.4rem, 4.1vw, 4rem);
    }

    .pilgrim-display-title--route .pilgrim-display-title__solid {
      font-size: clamp(2rem, 3.5vw, 3.4rem);
    }

    .pilgrim-display-title--route .pilgrim-display-title__script {
      font-size: clamp(2.25rem, 3.6vw, 3.5rem);
    }

    .pilgrim-route__map {
      max-width: 520px;
    }

    .pilgrim-route__grid > .pilgrim-facts--panel article {
      min-height: 92px;
      padding: 14px;
    }

    .pilgrim-route__stops span:nth-child(n+7) {
      display: none;
    }

    .pilgrim-display-title--itinerary .pilgrim-display-title__outline,
    .pilgrim-display-title--includes .pilgrim-display-title__outline {
      font-size: clamp(2.2rem, 4vw, 3.75rem);
    }

    .pilgrim-display-title--itinerary .pilgrim-display-title__solid,
    .pilgrim-display-title--includes .pilgrim-display-title__solid {
      font-size: clamp(1.95rem, 3.4vw, 3.1rem);
    }

    .pilgrim-display-title--itinerary .pilgrim-display-title__script,
    .pilgrim-display-title--includes .pilgrim-display-title__script {
      font-size: clamp(2.05rem, 3.3vw, 3rem);
    }

    .pilgrim-itinerary__grid .pilgrim-section-head--story p:not(.pilgrim-kicker),
    .pilgrim-includes-panel__grid .pilgrim-section-head--story p:not(.pilgrim-kicker) {
      font-size: 0.9rem;
      line-height: 1.36;
    }

    .pilgrim-day--preview {
      min-height: 150px;
      padding: 14px;
    }

    .pilgrim-hotels__list article {
      min-height: 132px;
    }

    .pilgrim-overview-grid article {
      min-height: 210px;
      padding: 16px;
    }

    .pilgrim-overview-icon {
      width: 38px;
      height: 38px;
      margin-bottom: 12px;
      font-size: 1.1rem;
    }

    .pilgrim-overview-grid ul {
      gap: 8px;
      margin-top: 12px;
    }

    .pilgrim-overview-grid li {
      font-size: 0.78rem;
      line-height: 1.26;
    }

    .pilgrim-overview-grid li:nth-child(n+4) {
      display: none;
    }

    .pilgrim-flight-notes p {
      min-height: 118px;
      padding-top: 18px;
      padding-bottom: 18px;
    }
  }

  @media (max-width: 980px) {
    .pilgrim-route__grid,
    .pilgrim-itinerary__grid,
    .pilgrim-hotels__grid,
    .pilgrim-includes-panel__grid,
    .pilgrim-flights__content {
      grid-template-columns: 1fr;
    }

    .pilgrim-route__content,
    .pilgrim-itinerary__grid .pilgrim-section-head--story,
    .pilgrim-includes-panel__grid .pilgrim-section-head--story,
    .pilgrim-hotels__grid > div:first-child,
    .pilgrim-flights__intro,
    .pilgrim-final__box {
      text-align: left;
    }

    .pilgrim-days--preview,
    .pilgrim-overview-grid,
    .pilgrim-hotels__list {
      display: flex;
      grid-template-columns: none;
      overflow-x: auto;
      scroll-snap-type: x mandatory;
      scrollbar-width: none;
    }

    .pilgrim-day--preview,
    .pilgrim-overview-grid article,
    .pilgrim-hotels__list article {
      flex: 0 0 min(82vw, 360px);
      scroll-snap-align: start;
    }

    .pilgrim-flight-notes {
      grid-template-columns: 1fr;
    }
  }

  @media (max-width: 680px) {
    .pilgrim-route__content,
    .pilgrim-itinerary__grid .pilgrim-section-head--story,
    .pilgrim-includes-panel__grid .pilgrim-section-head--story,
    .pilgrim-hotels__grid > div:first-child,
    .pilgrim-flights__intro,
    .pilgrim-final__box {
      text-align: left;
    }

    .pilgrim-display-title--route,
    .pilgrim-display-title--itinerary,
    .pilgrim-display-title--includes,
    .pilgrim-display-title--hotels,
    .pilgrim-display-title--flights,
    .pilgrim-display-title--final {
      justify-items: start;
      margin-inline: 0;
    }

    .pilgrim-route__map {
      display: block;
      max-width: 100%;
    }

    .pilgrim-route__stops {
      display: none;
    }

    .pilgrim-final__actions {
      padding: 0;
      border: 0;
      background: transparent;
    }

    .pilgrim-flight-notes p {
      min-height: 0;
      padding: 18px 18px 18px 62px;
    }
  }

  .pilgrim-route .pilgrim-panel-bg,
  .pilgrim-hotels .pilgrim-panel-bg,
  .pilgrim-final .pilgrim-panel-bg {
    background:
      linear-gradient(90deg, rgba(7, 17, 31, 0.98) 0%, rgba(23, 36, 60, 0.9) 45%, rgba(7, 17, 31, 0.78) 100%),
      url("/images/peregrinaciones/turquia-islas-griegas-2026/istanbul-cruise.jpg") center / cover;
    opacity: 0.86;
  }

  .pilgrim-route .pilgrim-panel-bg::after,
  .pilgrim-hotels .pilgrim-panel-bg::after,
  .pilgrim-final .pilgrim-panel-bg::after {
    content: "";
    position: absolute;
    inset: 0;
    background:
      linear-gradient(rgba(255, 255, 255, 0.025) 1px, transparent 1px),
      linear-gradient(90deg, rgba(255, 255, 255, 0.025) 1px, transparent 1px);
    background-size: 62px 62px;
    mask-image: linear-gradient(180deg, rgba(0, 0, 0, 0.7), transparent 82%);
  }

  .pilgrim-route__motifs {
    display: grid;
    width: min(100%, 920px);
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 10px;
    margin-top: 24px;
  }

  .pilgrim-route__motifs article {
    min-height: 96px;
    padding: 16px 18px;
    border: 1px solid rgba(255, 255, 255, 0.2);
    border-radius: 30px 30px 8px 30px;
    background: rgba(255, 255, 255, 0.08);
    text-align: left;
    backdrop-filter: blur(14px);
  }

  .pilgrim-route__motifs strong {
    display: block;
    color: var(--pilgrim-teal);
    font-size: 0.74rem;
    font-weight: 950;
    letter-spacing: 0.18em;
    text-transform: uppercase;
  }

  .pilgrim-route__motifs span {
    display: block;
    margin-top: 8px;
    color: rgba(255, 255, 255, 0.78);
    font-size: 0.82rem;
    line-height: 1.36;
  }

  .pilgrim-route__map {
    border-radius: 38px 38px 10px 38px;
    isolation: isolate;
  }

  .pilgrim-route__map::before {
    content: "";
    position: absolute;
    inset: 16px;
    z-index: 1;
    border: 1px solid rgba(255, 255, 255, 0.34);
    border-radius: 28px 28px 8px 28px;
    pointer-events: none;
  }

  .pilgrim-route__map-caption {
    position: absolute;
    left: 28px;
    top: 28px;
    z-index: 2;
    display: inline-flex;
    min-height: 38px;
    align-items: center;
    padding: 0 16px;
    border-radius: 999px;
    background: rgba(7, 17, 31, 0.74);
    color: white;
    font-size: 0.72rem;
    font-weight: 950;
    letter-spacing: 0.16em;
    text-transform: uppercase;
    backdrop-filter: blur(14px);
  }

  .pilgrim-route__grid > .pilgrim-facts--panel {
    gap: 12px;
    border: 0;
    background: transparent;
  }

  .pilgrim-route__grid > .pilgrim-facts--panel article {
    border: 1px solid rgba(255, 255, 255, 0.2);
    border-radius: 24px 24px 8px 24px;
    background: rgba(255, 255, 255, 0.095);
    backdrop-filter: blur(14px);
  }

  .pilgrim-hotels {
    position: relative;
    isolation: isolate;
  }

  .pilgrim-hotels::before {
    content: "DESCANSO";
    position: absolute;
    right: -0.18em;
    bottom: -0.18em;
    z-index: 0;
    color: rgba(255, 255, 255, 0.035);
    font-size: clamp(7rem, 20vw, 19rem);
    font-weight: 950;
    letter-spacing: 0;
    line-height: 0.8;
    pointer-events: none;
  }

  .pilgrim-hotels__list {
    counter-reset: hotel-stop;
  }

  .pilgrim-hotels__list article {
    position: relative;
    overflow: hidden;
    display: grid;
    grid-template-columns: auto minmax(0, 1fr);
    gap: 16px;
    align-items: center;
    border: 1px solid rgba(255, 255, 255, 0.18);
    border-radius: 34px 34px 10px 34px;
    background:
      linear-gradient(145deg, rgba(255, 255, 255, 0.15), rgba(255, 255, 255, 0.055)),
      rgba(7, 17, 31, 0.36);
    box-shadow: 0 28px 74px rgba(0, 0, 0, 0.18);
    backdrop-filter: blur(18px);
  }

  .pilgrim-hotels__list article::after {
    content: "";
    position: absolute;
    right: -22px;
    bottom: -22px;
    width: 96px;
    height: 96px;
    border: 1px solid rgba(8, 174, 190, 0.24);
    border-radius: 999px;
    background: rgba(8, 174, 190, 0.08);
  }

  .pilgrim-hotels__list article:nth-child(even) {
    transform: translateY(18px);
  }

  .pilgrim-hotels__list em {
    position: relative;
    z-index: 1;
    display: grid;
    width: 56px;
    height: 56px;
    place-items: center;
    border-radius: 999px;
    background: var(--pilgrim-teal);
    color: var(--pilgrim-deep);
    font-style: normal;
    font-size: 0.92rem;
    font-weight: 950;
  }

  .pilgrim-hotels__list article > div {
    position: relative;
    z-index: 1;
    min-width: 0;
  }

  .pilgrim-hotels__list strong {
    color: white;
    font-size: 1.08rem;
  }

  .pilgrim-coverage-board {
    display: grid;
    grid-template-columns: minmax(0, 1.1fr) minmax(0, 1fr) minmax(240px, 0.86fr);
    gap: 14px;
    width: 100%;
  }

  .pilgrim-coverage-card {
    position: relative;
    min-height: 316px;
    overflow: hidden;
    padding: 26px;
    border: 1px solid rgba(255, 255, 255, 0.18);
    border-radius: 34px 34px 10px 34px;
    background:
      linear-gradient(145deg, rgba(255, 255, 255, 0.14), rgba(255, 255, 255, 0.05)),
      rgba(255, 255, 255, 0.06);
    box-shadow: 0 30px 80px rgba(0, 0, 0, 0.18);
  }

  .pilgrim-coverage-card::after {
    content: "";
    position: absolute;
    right: -42px;
    top: -42px;
    width: 150px;
    height: 150px;
    border: 1px solid rgba(255, 255, 255, 0.22);
    border-radius: 999px;
    background: rgba(255, 255, 255, 0.055);
  }

  .pilgrim-coverage-card--turkey {
    background: var(--pilgrim-teal);
    color: var(--pilgrim-deep);
  }

  .pilgrim-coverage-card--turkey .pilgrim-overview-icon,
  .pilgrim-coverage-card--turkey .pilgrim-overview-label,
  .pilgrim-coverage-card--turkey li {
    color: var(--pilgrim-deep);
  }

  .pilgrim-coverage-card--turkey .pilgrim-overview-icon {
    border-color: rgba(7, 17, 31, 0.22);
    background: rgba(255, 255, 255, 0.22);
  }

  .pilgrim-coverage-card--apart {
    background: rgba(255, 255, 255, 0.94);
    color: var(--pilgrim-deep);
  }

  .pilgrim-coverage-card--apart .pilgrim-overview-icon,
  .pilgrim-coverage-card--apart .pilgrim-overview-label {
    color: #087f8b;
  }

  .pilgrim-coverage-card h3 {
    position: relative;
    z-index: 1;
    margin: 12px 0 20px;
    color: currentColor;
    font-size: clamp(1.35rem, 2vw, 1.85rem);
    line-height: 1.02;
  }

  .pilgrim-coverage-card ul {
    position: relative;
    z-index: 1;
    display: grid;
    gap: 11px;
    margin: 0;
    padding: 0;
    list-style: none;
  }

  .pilgrim-coverage-card li {
    position: relative;
    padding-left: 22px;
    color: rgba(255, 255, 255, 0.78);
    font-size: 0.88rem;
    line-height: 1.34;
  }

  .pilgrim-coverage-card--apart li {
    color: rgba(16, 24, 39, 0.76);
  }

  .pilgrim-coverage-card li::before {
    content: "";
    position: absolute;
    left: 0;
    top: 0.48em;
    width: 8px;
    height: 8px;
    border-radius: 999px;
    background: currentColor;
  }

  .pilgrim-final {
    position: relative;
    isolation: isolate;
    overflow: hidden;
  }

  .pilgrim-final::before {
    content: "2026";
    position: absolute;
    left: 50%;
    top: 50%;
    z-index: 0;
    color: rgba(255, 255, 255, 0.14);
    font-size: clamp(12rem, 34vw, 34rem);
    font-weight: 950;
    letter-spacing: 0;
    line-height: 0.8;
    transform: translate(-50%, -46%);
    pointer-events: none;
  }

  .pilgrim-final .pilgrim-kicker--navy {
    color: var(--pilgrim-teal);
  }

  .pilgrim-final .pilgrim-display-title--final .pilgrim-display-title__outline {
    -webkit-text-stroke-color: rgba(255, 255, 255, 0.76);
    text-stroke-color: rgba(255, 255, 255, 0.76);
  }

  .pilgrim-final .pilgrim-display-title--final .pilgrim-display-title__solid {
    color: white;
  }

  .pilgrim-final .pilgrim-display-title--final .pilgrim-display-title__script {
    color: var(--pilgrim-teal);
  }

  .pilgrim-final p {
    color: rgba(255, 255, 255, 0.78);
  }

  .pilgrim-final__contact {
    display: grid;
    gap: 18px;
    justify-items: center;
  }

  .pilgrim-final__stamp {
    display: grid;
    min-width: min(100%, 280px);
    padding: 26px 28px;
    border: 1px solid rgba(23, 36, 60, 0.24);
    border-radius: 38px 38px 12px 38px;
    background: rgba(255, 255, 255, 0.26);
    box-shadow: 0 28px 72px rgba(7, 17, 31, 0.16);
    text-align: left;
    backdrop-filter: blur(14px);
  }

  .pilgrim-final__stamp span,
  .pilgrim-final__stamp em {
    color: rgba(23, 36, 60, 0.72);
    font-size: 0.78rem;
    font-style: normal;
    font-weight: 950;
    letter-spacing: 0.18em;
    text-transform: uppercase;
  }

  .pilgrim-final__stamp strong {
    margin: 4px 0 0;
    color: var(--pilgrim-navy);
    font-size: clamp(2.2rem, 4vw, 3.6rem);
    line-height: 0.88;
    text-transform: uppercase;
  }

  .pilgrim-flights__shade {
    background:
      linear-gradient(90deg, rgba(7, 17, 31, 0.98) 0%, rgba(23, 36, 60, 0.92) 42%, rgba(23, 36, 60, 0.82) 100%),
      linear-gradient(0deg, rgba(7, 17, 31, 0.94), rgba(7, 17, 31, 0.2));
  }

  @media (min-width: 981px) {
    .pilgrim-panel--route .pilgrim-panel-inner,
    .pilgrim-panel--includes .pilgrim-panel-inner {
      padding-top: 72px;
      padding-bottom: 44px;
    }

    .pilgrim-route__grid {
      grid-template-columns: minmax(420px, 0.94fr) minmax(340px, 0.78fr);
      grid-template-areas:
        "route-head route-head"
        "route-map route-facts"
        "route-stops route-stops";
    }

    .pilgrim-route__content {
      max-width: 980px;
    }

    .pilgrim-hotels__grid {
      grid-template-columns: minmax(0, 0.72fr) minmax(0, 1.28fr);
      text-align: left;
    }

    .pilgrim-hotels__grid > div:first-child {
      justify-items: start;
      text-align: left;
    }

    .pilgrim-hotels__grid > div:first-child p {
      margin-inline: 0;
    }

    .pilgrim-display-title--hotels {
      justify-items: start;
      margin-inline: 0;
    }

    .pilgrim-hotels__list {
      padding: 18px 0 28px;
    }

    .pilgrim-includes-panel__side {
      width: min(100%, 1160px);
    }

    .pilgrim-coverage-card {
      min-height: 286px;
      padding: 22px;
    }

    .pilgrim-coverage-card h3 {
      margin-bottom: 16px;
    }

    .pilgrim-final__box {
      grid-template-columns: minmax(0, 0.9fr) minmax(320px, 0.58fr);
      justify-items: stretch;
      text-align: left;
    }

    .pilgrim-final__box > div:first-child {
      justify-items: start;
    }

    .pilgrim-final p {
      margin-inline: 0;
    }

    .pilgrim-final__actions {
      border-radius: 34px 34px 12px 34px;
    }
  }

  @media (min-width: 981px) and (max-height: 820px) {
    .pilgrim-route__motifs {
      display: none;
    }

    .pilgrim-panel--includes .pilgrim-panel-inner {
      padding-top: 58px;
      padding-bottom: 28px;
    }

    .pilgrim-includes-panel__grid {
      gap: 16px;
    }

    .pilgrim-display-title--includes .pilgrim-display-title__outline {
      font-size: clamp(2.35rem, 4.45vw, 4.35rem);
    }

    .pilgrim-display-title--includes .pilgrim-display-title__solid {
      font-size: clamp(2rem, 3.72vw, 3.45rem);
    }

    .pilgrim-display-title--includes .pilgrim-display-title__script {
      font-size: clamp(2.15rem, 3.5vw, 3.2rem);
    }

    .pilgrim-includes-panel__grid .pilgrim-section-head--story p:not(.pilgrim-kicker) {
      font-size: 0.86rem;
      line-height: 1.32;
    }

    .pilgrim-coverage-card {
      min-height: 220px;
      padding: 18px;
    }

    .pilgrim-coverage-card h3 {
      margin: 8px 0 12px;
      font-size: 1.18rem;
    }

    .pilgrim-coverage-card li {
      font-size: 0.78rem;
      line-height: 1.26;
    }

    .pilgrim-coverage-card li:nth-child(n+4) {
      display: none;
    }

    .pilgrim-panel--includes .pilgrim-overview-icon {
      width: 38px;
      height: 38px;
      margin-bottom: 10px;
      font-size: 1.1rem;
    }

    .pilgrim-panel--includes .pilgrim-actions {
      margin-top: 10px;
    }

    .pilgrim-panel--includes .pilgrim-button {
      min-height: 46px;
      padding-inline: 22px;
      font-size: 0.74rem;
    }
  }

  @media (min-width: 981px) and (max-height: 740px) {
    .pilgrim-panel--includes .pilgrim-includes-panel__side {
      gap: 0;
    }

    .pilgrim-panel--includes .pilgrim-actions {
      margin-top: -2px;
    }

    .pilgrim-route__motifs {
      margin-top: 14px;
    }

    .pilgrim-route__motifs article {
      min-height: 46px;
      padding: 12px 14px;
      border-radius: 22px 22px 8px 22px;
    }

    .pilgrim-route__motifs span {
      display: none;
    }

    .pilgrim-hotels__list article {
      min-height: 118px;
    }

    .pilgrim-hotels__list article:nth-child(even) {
      transform: translateY(10px);
    }

    .pilgrim-coverage-card {
      min-height: 190px;
      padding: 18px;
    }

    .pilgrim-coverage-card h3 {
      margin-bottom: 12px;
      font-size: 1.18rem;
    }

    .pilgrim-coverage-card li {
      font-size: 0.78rem;
      line-height: 1.26;
    }

    .pilgrim-coverage-card li:nth-child(n+4) {
      display: none;
    }

    .pilgrim-final__stamp {
      padding: 18px 22px;
    }
  }

  @media (max-width: 980px) {
    .pilgrim-route__motifs,
    .pilgrim-coverage-board {
      grid-template-columns: 1fr;
    }

    .pilgrim-hotels__list article:nth-child(even) {
      transform: none;
    }

    .pilgrim-coverage-card {
      min-height: 0;
    }

    .pilgrim-final__contact {
      justify-items: stretch;
    }
  }

  @media (max-width: 680px) {
    .pilgrim-route .pilgrim-panel-bg,
    .pilgrim-hotels .pilgrim-panel-bg {
      opacity: 0.58;
    }

    .pilgrim-route__motifs {
      gap: 8px;
      margin-top: 18px;
    }

    .pilgrim-route__motifs article {
      min-height: 0;
      padding: 14px;
      border-radius: 24px 24px 8px 24px;
    }

    .pilgrim-route__motifs span {
      font-size: 0.8rem;
    }

    .pilgrim-route__map-caption {
      left: 18px;
      top: 18px;
      min-height: 34px;
      font-size: 0.64rem;
    }

    .pilgrim-hotels__list article,
    .pilgrim-coverage-card,
    .pilgrim-final__stamp {
      border-radius: 28px 28px 10px 28px;
    }

    .pilgrim-hotels__list em {
      width: 48px;
      height: 48px;
      font-size: 0.82rem;
    }

    .pilgrim-coverage-card li:nth-child(n+5) {
      display: none;
    }

    .pilgrim-final__stamp {
      min-width: 0;
    }
  }

  @media (max-width: 820px) {
    :global(html[data-cumbre-static-story='true']) .pilgrim-page .cumbre-panel {
      min-height: auto;
    }

    :global(html[data-cumbre-static-story='true']) .pilgrim-panel-inner {
      min-height: auto;
    }

    .pilgrim-panel-inner {
      padding-top: 110px;
      padding-bottom: 70px;
    }

    .pilgrim-route .pilgrim-panel-inner {
      padding-top: 30px;
    }

    .pilgrim-route__grid,
    .pilgrim-itinerary__grid,
    .pilgrim-hotels__grid,
    .pilgrim-includes-panel__grid {
      gap: 28px;
    }

    .pilgrim-route__content {
      max-width: 100%;
    }

    .pilgrim-route__content p:not(.pilgrim-kicker) {
      max-width: 34rem;
      font-size: clamp(1rem, 3.7vw, 1.18rem);
      line-height: 1.48;
    }

    .pilgrim-display-title--route .pilgrim-display-title__outline {
      font-size: clamp(2.55rem, 10vw, 3.45rem);
    }

    .pilgrim-display-title--route .pilgrim-display-title__solid {
      font-size: clamp(2.25rem, 8.5vw, 3.15rem);
    }

    .pilgrim-display-title--route .pilgrim-display-title__script {
      font-size: clamp(2.35rem, 8.5vw, 3.2rem);
    }

    .pilgrim-route__motifs {
      display: none;
    }

    .pilgrim-route__map {
      width: min(100%, 460px);
      max-width: 100%;
      margin-inline: auto;
      border-radius: 30px 30px 10px 30px;
    }

    .pilgrim-route__map::before {
      inset: 12px;
      border-radius: 22px 22px 8px 22px;
    }

    .pilgrim-route__map-caption {
      left: 22px;
      top: 22px;
      min-height: 36px;
      padding-inline: 14px;
      font-size: 0.68rem;
    }

    .pilgrim-route__grid > .pilgrim-facts--panel {
      width: min(100%, 520px);
      margin: 0 auto;
      grid-template-columns: repeat(2, minmax(0, 1fr));
      gap: 10px;
    }

    .pilgrim-route__grid > .pilgrim-facts--panel article {
      min-height: 98px;
      padding: 14px;
      border-radius: 24px 24px 8px 24px;
      background: rgba(7, 17, 31, 0.62);
      backdrop-filter: blur(16px);
    }

    .pilgrim-route__grid > .pilgrim-facts--panel .pilgrim-fact-icon {
      width: 34px;
      height: 34px;
      margin-bottom: 10px;
    }

    .pilgrim-route__grid > .pilgrim-facts--panel strong {
      font-size: clamp(0.98rem, 3.8vw, 1.18rem);
    }
  }

  @media (max-width: 560px) {
    .pilgrim-panel-inner {
      padding-top: 102px;
      padding-bottom: 60px;
    }

    .pilgrim-route__grid > .pilgrim-facts--panel {
      grid-template-columns: 1fr;
    }

    .pilgrim-route__grid > .pilgrim-facts--panel article {
      min-height: 86px;
    }
  }

  @media (min-width: 521px) and (max-width: 820px) {
    .pilgrim-panel-inner {
      height: var(--cumbre-vh, 100svh);
      min-height: var(--cumbre-vh, 100svh);
      padding: 64px 0 34px;
    }

    .pilgrim-panel--route .pilgrim-panel-inner,
    .pilgrim-panel--itinerary .pilgrim-panel-inner,
    .pilgrim-panel--hotels .pilgrim-panel-inner,
    .pilgrim-panel--includes .pilgrim-panel-inner,
    .pilgrim-panel--final .pilgrim-panel-inner {
      padding-top: 62px;
      padding-bottom: 32px;
    }

    .pilgrim-kicker {
      margin-bottom: 8px;
      font-size: 0.66rem;
      letter-spacing: 0.28em;
    }

    .pilgrim-route__grid,
    .pilgrim-itinerary__grid,
    .pilgrim-hotels__grid,
    .pilgrim-includes-panel__grid {
      gap: 12px;
      align-content: center;
    }

    .pilgrim-hero__grid {
      gap: 14px;
      align-content: center;
    }

    .pilgrim-hero__lead,
    .pilgrim-route__content p:not(.pilgrim-kicker),
    .pilgrim-hotels p,
    .pilgrim-final p,
    .pilgrim-section-head p:not(.pilgrim-kicker):not(.pilgrim-script) {
      max-width: 34rem;
      margin-top: 8px;
      font-size: 0.82rem;
      line-height: 1.34;
    }

    .pilgrim-display-title__outline {
      font-size: clamp(2.05rem, 7.6vw, 3rem);
    }

    .pilgrim-display-title__solid {
      font-size: clamp(1.78rem, 6.5vw, 2.55rem);
    }

    .pilgrim-display-title__script {
      font-size: clamp(1.95rem, 7vw, 2.8rem);
    }

    .pilgrim-video-card {
      width: min(100%, 360px);
      padding: 8px;
    }

    .pilgrim-video-card__topline {
      min-height: 28px;
      padding-bottom: 6px;
      font-size: 0.54rem;
    }

    .pilgrim-video-card__topline strong {
      min-height: 24px;
      padding-inline: 9px;
      font-size: 0.54rem;
    }

    .pilgrim-route__motifs,
    .pilgrim-route__stops,
    .pilgrim-poster {
      display: none;
    }

    .pilgrim-route__map {
      width: min(100%, 360px);
      max-width: 360px;
      margin-inline: auto;
      border-radius: 24px 24px 8px 24px;
    }

    .pilgrim-route__map::before {
      inset: 10px;
      border-radius: 18px 18px 6px 18px;
    }

    .pilgrim-route__map-caption {
      min-height: 30px;
      left: 18px;
      top: 18px;
      padding-inline: 12px;
      font-size: 0.6rem;
      letter-spacing: 0.12em;
    }

    .pilgrim-route__grid > .pilgrim-facts--panel {
      width: min(100%, 430px);
      grid-template-columns: repeat(2, minmax(0, 1fr));
      gap: 8px;
      margin: 0 auto;
    }

    .pilgrim-route__grid > .pilgrim-facts--panel article {
      min-height: 62px;
      padding: 10px 12px;
      border-radius: 18px 18px 6px 18px;
      background: rgba(7, 17, 31, 0.68);
    }

    .pilgrim-route__grid > .pilgrim-facts--panel .pilgrim-fact-icon {
      display: none;
    }

    .pilgrim-route__grid > .pilgrim-facts--panel .pilgrim-fact-label {
      font-size: 0.56rem;
      letter-spacing: 0.14em;
    }

    .pilgrim-route__grid > .pilgrim-facts--panel strong {
      margin-top: 5px;
      font-size: 0.9rem;
      line-height: 1.12;
    }

    .pilgrim-day--preview {
      flex-basis: min(72vw, 260px);
      min-height: 132px;
      padding: 13px;
    }

    .pilgrim-hotels__list article {
      flex-basis: min(72vw, 270px);
      min-height: 126px;
      padding: 16px;
    }

    .pilgrim-hotels__list article:nth-child(even) {
      transform: none;
    }

    .pilgrim-coverage-board {
      display: flex;
      gap: 10px;
      overflow-x: auto;
      padding-bottom: 8px;
      scroll-snap-type: x mandatory;
      scrollbar-width: none;
      width: 100%;
    }

    .pilgrim-coverage-board::-webkit-scrollbar {
      display: none;
    }

    .pilgrim-coverage-card {
      flex: 0 0 calc((100% - 10px) / 2);
      min-height: 190px;
      min-width: 0;
      padding: 16px;
      scroll-snap-align: start;
    }

    .pilgrim-coverage-card h3 {
      margin: 8px 0 10px;
      font-size: 1.1rem;
    }

    .pilgrim-coverage-card li {
      font-size: 0.74rem;
      line-height: 1.24;
    }

    .pilgrim-coverage-card li:nth-child(n+4) {
      display: none;
    }

    .pilgrim-panel--includes .pilgrim-actions {
      margin-top: 6px;
    }

    .pilgrim-final__box {
      grid-template-columns: 1fr;
      gap: 14px;
      text-align: left;
    }

    .pilgrim-final__box > div:first-child {
      justify-items: start;
    }

    .pilgrim-final__contact {
      grid-template-columns: minmax(160px, 0.7fr) minmax(220px, 1fr);
      align-items: center;
      gap: 12px;
    }

    .pilgrim-final__stamp {
      min-width: 0;
      padding: 16px 18px;
    }

    .pilgrim-final__stamp strong {
      font-size: 2rem;
    }

    .pilgrim-final__actions {
      justify-content: stretch;
      padding: 8px;
      border-radius: 24px 24px 8px 24px;
      background: rgba(7, 17, 31, 0.28);
    }

    .pilgrim-button {
      min-height: 44px;
      padding-inline: 18px;
      font-size: 0.7rem;
    }
  }
</style>
